const PHASE_NETWORKING = {
  name:"Phase 10 · Networking & CDN",id:"network",icon:"🌐",phase:"Networking",color:"#0891b2",
  sizes:["small","medium","large","hyper"],
  short:"The invisible substrate — DNS, TLS, CDN, service mesh, and transport protocols that determine the floor of your latency and the ceiling of your reliability",
  detail:{
    what:"The networking layer is everything that moves bytes between users and your servers, and between your services internally. It encompasses five critical sublayers: (1) DNS — the global phone book that translates human-readable domain names (api.example.com) into IP addresses. DNS is the very first thing that happens when a user types your URL; if it's slow or broken, nothing else matters. (2) TLS — the encryption layer that secures data in transit. TLS 1.3 (the current standard) completes a handshake in one round-trip (vs two for TLS 1.2), saving 50–100ms on every new connection. Without TLS, anyone on the network path — ISPs, coffee shop WiFi operators, government surveillance — can read and modify your traffic. (3) CDN (Content Delivery Network) — a global network of edge servers that cache your content close to users. When a user in Mumbai requests your JavaScript bundle, the CDN serves it from a server in Mumbai (~5ms) instead of from your origin in Virginia (~250ms). CDNs are the single most cost-effective latency optimization in existence. (4) Service mesh — a dedicated infrastructure layer for service-to-service communication that handles mTLS encryption, load balancing, circuit breaking, retries, and observability without any changes to application code. Think of it as a 'networking sidecar' attached to every service that handles all the cross-cutting concerns. (5) Transport protocols — HTTP/1.1 (text-based, one request per TCP connection at a time), HTTP/2 (binary, multiplexed — many requests share one connection), HTTP/3 (QUIC over UDP — eliminates TCP head-of-line blocking, dramatically better on lossy mobile networks). Each protocol generation recovers 10–30% of wasted latency from its predecessor. The networking layer is invisible when it works and catastrophic when it fails. It's the substrate every other component sits on — your perfectly tuned database and elegantly designed API are irrelevant if the network between user and server is slow, unreliable, or insecure.",
    why:"Network is simultaneously the last mile of latency (the irreducible minimum time for bits to travel between user and server) and the first mile of unreliability (the component most likely to experience intermittent failures, packet loss, and variable performance). A perfectly optimized backend served from us-east-1 still adds 150ms for a user in Singapore — the speed of light in fiber is approximately 5ms per 1,000km, and Virginia to Singapore is ~15,000km. No amount of code optimization, database tuning, or caching can overcome physics. CDN closes that gap by caching content at Points of Presence (PoPs) near users — Cloudflare has 300+ PoPs, CloudFront has 400+, serving cached content in 5–30ms from almost any city on Earth. Service mesh makes internal service-to-service calls observable (distributed tracing for every internal request), secure (mTLS so a compromised service can't sniff others' traffic), and resilient (automatic retries, circuit breaking, load balancing) — all without per-service code changes. HTTP/3/QUIC reclaims latency on flaky mobile networks: on a connection with 2% packet loss (common on mobile), HTTP/2 over TCP degrades severely because a single lost packet blocks ALL multiplexed streams (head-of-line blocking). HTTP/3 over QUIC (UDP) isolates streams so only the affected stream waits. Google measured 8% faster page loads on mobile just from switching to QUIC. Underinvest in networking and your fast application feels slow to users — they blame your product, not the network. Overinvest and you maintain complex infrastructure (BGP routing, custom PoP networks) you don't need. The right investment level depends on where your users are, how latency-sensitive your product is, and how large your engineering team is.",
    numbers:"Speed of light in fiber: ~5ms per 1,000km one-way, ~10ms round-trip, with real-world routing adding ~2× overhead (so ~20ms per 1,000km RTT in practice). Key routes: NYC↔London ~70ms RTT, NYC↔Tokyo ~170ms, NYC↔Sydney ~250ms, NYC↔Mumbai ~230ms. CDN cache hit latency: 1–10ms from local PoP (user to nearest CDN edge), 10–30ms for regional cache (edge to regional hub). CDN cache miss: full round-trip to origin, same as no CDN. Target CDN cache hit rate: >90% for static assets, >60% for dynamic cacheable responses. TLS handshake cost: TLS 1.2 = 2 RTTs (~140ms on a 70ms path), TLS 1.3 = 1 RTT (~70ms), TLS 1.3 with 0-RTT resumption = 0 RTTs (instant for returning users, but replay-attackable so use only for idempotent requests). HTTP/2 saves ~1 RTT vs HTTP/1.1 per page load through multiplexing (many requests on one connection vs one-at-a-time). HTTP/3 (QUIC) saves another RTT on connection setup (combines TLS + transport handshake) and eliminates head-of-line blocking — measured improvements: 3–8% faster page loads on good networks, 10–30% faster on lossy mobile networks. Service mesh overhead: sidecar proxy (Envoy) adds 0.5–2ms per hop in the request path. For a request traversing 5 services, that's 2.5–10ms of added latency — a real cost, but the observability and security benefits typically justify it at scale. mTLS handshake between services: <1ms with session resumption (amortized across many requests on persistent connections).",
    keyInsight:"Networking is the one layer where physics sets a hard floor. You can optimize code infinitely, but you cannot make light travel faster. Every networking decision is ultimately about minimizing the distance (physical or logical) between the data and the user who needs it. CDN puts static data close. Edge compute puts logic close. Multi-region deployment puts everything close. The sophistication of your approach should match the gap between your latency SLO and the physics floor for your user distribution."
  },
  tradeoffs:[
    {axis:"Simplicity vs Performance",left:"Single endpoint in one region: simple to operate, one set of infrastructure, but distant users pay speed-of-light penalty on every request",right:"Anycast with 200+ PoPs: complex to operate (BGP, edge config management, cache coherence), but sub-30ms first-byte from anywhere on Earth",pos:0.5},
    {axis:"Cost vs Coverage",left:"Fewer PoPs / single CDN: cheaper monthly bill, but higher latency for users far from PoPs and single-vendor risk",right:"More PoPs / multi-CDN: lower latency everywhere, vendor redundancy, but higher cost and operational complexity of managing multiple CDN configs",pos:0.5},
    {axis:"Encryption everywhere vs Performance",left:"mTLS on every internal call: zero-trust security, no plaintext anywhere, but adds CPU overhead and operational complexity (certificate management, rotation)",right:"TLS only at edge, plaintext internal: faster internal communication, simpler ops, but a compromised service inside your VPC can sniff all internal traffic",pos:0.4}
  ],
  pitfalls:[
    {name:"No CDN for static assets",desc:"Your JS bundle (500KB) and hero image (2MB) are served directly from your origin server in us-east-1. A user in Mumbai experiences: DNS lookup (~50ms) + TCP handshake (~230ms RTT) + TLS handshake (~230ms) + data transfer (~460ms for 2.5MB at ~5Mbps effective) = ~970ms before the page renders. The same assets served from a CDN PoP in Mumbai: DNS (~5ms, CDN handles it) + TCP (~2ms local) + TLS (~2ms) + transfer (~50ms from local cache) = ~59ms. That's a 16× improvement for zero application code changes. CDN for static assets is universally correct and universally cheap — Cloudflare's free tier includes CDN, CloudFront charges ~$0.085/GB for the first 10TB. There is genuinely no reason not to use a CDN for static content. If you do nothing else from this phase, do this."},
    {name:"TLS termination only at the load balancer, then plaintext internally",desc:"Your architecture: User →(TLS)→ ALB →(plaintext HTTP)→ Service A →(plaintext)→ Service B →(plaintext)→ Database. An attacker who compromises any single service, or gains access to your VPC through a misconfigured security group, a stolen SSH key, or a supply-chain attack on a dependency, can now sniff all inter-service traffic — including auth tokens, PII, financial data, and internal API calls. This is not theoretical: the 2020 SolarWinds attack gave attackers inside-the-perimeter access to thousands of organizations. The fix: mTLS (mutual TLS) between all services, where each service has a certificate and both sides of every connection verify each other's identity. Service meshes (Istio, Linkerd) automate this — they manage certificate issuance, rotation, and enforcement without application code changes. The cost is 0.5–2ms per hop and some operational complexity. The alternative — 'we trust our VPC' — is the networking equivalent of leaving your front door unlocked because you live in a nice neighborhood."},
    {name:"DNS as a load balancer / failover mechanism",desc:"You use round-robin DNS to distribute traffic across servers, or DNS-based failover (change the A record when a server dies). The problem: DNS is aggressively cached at every layer — the ISP's recursive resolver, the OS resolver, the browser, and sometimes corporate proxies. Many clients ignore TTL entirely or enforce a minimum (some ISPs cache for 30+ minutes regardless of your 60-second TTL). When you change a DNS record for failover, 5–15 minutes may pass before all clients see the new IP — during which they're still hitting the dead server. For load balancing, DNS round-robin can't account for server health, connection count, or latency. Fix: use an actual load balancer (ALB, NLB, Envoy) for load distribution, and anycast or health-aware global load balancing (AWS Global Accelerator, Cloudflare) for geo-routing and failover. DNS should point to a stable entry point (the LB or anycast IP), not directly to servers."},
    {name:"Forgetting connection limits and ephemeral port exhaustion",desc:"Every TCP connection uses an ephemeral port on the client side (typically from a pool of ~28,000 ports on Linux). If each service-to-service call opens a new TCP connection and you're doing 10K internal RPS, you'll exhaust ephemeral ports in under 3 seconds (each connection holds a port for ~30s in TIME_WAIT). The symptoms: 'connection refused' errors that look like the downstream service is down, but it's actually the calling service that ran out of ports. Fixes: (1) Use HTTP/2 or gRPC which multiplex many requests over a single TCP connection — one connection can carry thousands of concurrent requests. (2) Enable TCP connection pooling in your HTTP client (keep-alive connections reused across requests). (3) Tune the kernel: reduce TIME_WAIT duration (net.ipv4.tcp_fin_timeout), enable port reuse (net.ipv4.tcp_tw_reuse). Most HTTP client libraries default to connection pooling, but many developers create a new client per request inadvertently (especially in serverless functions)."},
    {name:"BGP is not your friend if you're self-managing",desc:"Anycast routing via BGP (Border Gateway Protocol) sounds elegant: announce the same IP from multiple locations, and the internet's routing infrastructure automatically sends each user to the nearest one. But BGP is the routing protocol of the entire internet, and misconfiguration has catastrophic blast radius. Facebook's 6-hour outage in October 2021 was caused by a routine BGP maintenance operation that accidentally withdrew all of Facebook's route announcements — making Facebook, Instagram, WhatsApp, and even their internal tools (including the badge readers at their offices) completely unreachable worldwide. Pakistan accidentally hijacked YouTube's IP space via BGP in 2008, blacking out YouTube globally for hours. Unless you have a dedicated network engineering team with BGP experience, use Cloudflare, Fastly, or AWS Global Accelerator — they operate the anycast infrastructure and you consume it as a service."},
    {name:"Not enabling HTTP/2 (or still using HTTP/1.1 between services)",desc:"HTTP/1.1 allows only one request-response at a time per TCP connection. Browsers work around this by opening 6 parallel connections per domain — wasteful and limited. HTTP/2 multiplexes unlimited requests over a single connection, saving connection setup overhead and enabling server push. Enabling HTTP/2 is usually a one-line config change on your load balancer or reverse proxy (Nginx, Caddy, Envoy all support it). For service-to-service, gRPC uses HTTP/2 by default. There's almost no reason to use HTTP/1.1 for any connection in 2024+ — the performance improvement is free."}
  ],
  examples:[
    {name:"Cloudflare's 300+ PoPs — the network IS the product",desc:"Cloudflare operates 300+ Points of Presence in 100+ countries, reaching >95% of the world's internet-connected population within 50ms. This isn't just a CDN — it's a programmable edge network where you can run code (Workers), store data (R2, KV, Durable Objects), and implement security (WAF, DDoS protection, Bot Management) at every PoP. The competitive moat is physical: building a PoP requires negotiating peering agreements with local ISPs, installing hardware, and maintaining it — a multi-year, multi-billion dollar investment that new entrants can't replicate quickly. For users of Cloudflare: the free tier includes CDN, DNS, DDoS protection, and basic analytics. The Workers platform ($5/month for 10M requests) lets you run JavaScript/Rust at every PoP with <1ms cold start. This is the highest-value free infrastructure available to any startup."},
    {name:"Facebook 2021 BGP outage — when networking becomes existential",desc:"On October 4, 2021, a routine maintenance operation on Facebook's backbone network triggered a bug that withdrew all BGP route announcements for Facebook's IP address space. From the internet's perspective, Facebook ceased to exist — DNS queries for facebook.com returned nothing because Facebook's authoritative DNS servers were themselves unreachable (they were announced via the same BGP routes). The outage lasted 6 hours and affected Facebook, Instagram, WhatsApp, Messenger, and Oculus — approximately 3.5 billion users. Facebook employees couldn't fix the problem remotely because their internal tools also depended on the same network. Engineers had to physically travel to data centers and manually reset BGP configurations. The lesson is threefold: (1) Networking is invisible infrastructure until it catastrophically fails. (2) Circular dependencies (DNS depending on the same network it resolves) create unrecoverable states. (3) Always have an out-of-band access mechanism to your core infrastructure that doesn't depend on the infrastructure itself."},
    {name:"Google's QUIC protocol — rewriting the transport layer of the internet",desc:"Google developed QUIC (Quick UDP Internet Connections) starting in 2012 because TCP's design (from 1974) has fundamental limitations: head-of-line blocking (one lost packet stalls ALL streams on the connection), slow connection setup (TCP handshake + TLS handshake = 2–3 round trips before any data flows), and ossified middleboxes (firewalls and NATs that break when TCP is extended). QUIC runs over UDP, implements its own reliability and congestion control, integrates TLS 1.3 into the handshake (1 RTT to establish encrypted connection, 0 RTT for resumption), and isolates streams so a lost packet in one stream doesn't block others. Google measured: 8% reduction in rebuffering on YouTube, 3% faster Google Search on desktop, 5–10% faster on mobile. QUIC became HTTP/3 and is now standardized (RFC 9000). As of 2024, ~30% of web traffic uses HTTP/3. Enabling it is straightforward: Cloudflare enables it by default, Nginx supports it since 1.25, Caddy supports it natively. For service-to-service, quic-go (Go) and msquic (C) are production-ready libraries."}
  ],
  levelUp:[
    {from:"small",to:"medium",trigger:"Users in >1 geography or static assets total >1MB, or you notice that users in distant regions have significantly worse experience",action:"Step 1: Put Cloudflare (free tier) in front of your domain — this gives you instant CDN for static assets, DDoS protection, and automatic HTTP/2. Configure cache headers on your static assets: 'Cache-Control: public, max-age=31536000, immutable' for fingerprinted assets (style.abc123.css), 'Cache-Control: public, max-age=3600' for others. Step 2: Ensure your load balancer or reverse proxy is configured for HTTP/2 (Nginx: 'listen 443 ssl http2;'). Step 3: Verify TLS 1.3 is enabled and TLS 1.0/1.1 are disabled. Step 4: Add basic monitoring — Cloudflare's free analytics show cache hit rate, request distribution by country, and threat blocking. Target >80% cache hit rate on static assets. This entire process takes 1–2 hours and costs $0."},
    {from:"medium",to:"large",trigger:">20% of traffic originates from outside your home region, or latency complaints from distant users despite CDN",action:"Step 1: Deploy compute in a second region (e.g., if primary is us-east-1 and 25% of traffic is from Europe, add eu-west-1). Use Terraform to codify the second region's infrastructure identically. Step 2: Set up GeoDNS (Route53 latency-based routing or Cloudflare load balancing) to route users to the nearest region. Step 3: Deploy edge workers (Cloudflare Workers or Lambda@Edge) for dynamic personalization at the edge — A/B test assignments, locale detection, auth token validation — avoiding the round-trip to origin for these lightweight operations. Step 4: If you have >10 internal services, evaluate a service mesh (start with Linkerd — lighter than Istio, easier to adopt: 'linkerd install | kubectl apply -f -' gets you mTLS and observability in minutes). Step 5: Enable HTTP/3 on your edge (Cloudflare does this automatically; if using CloudFront, enable it in distribution settings). Monitor: per-region latency dashboards, CDN cache hit rates by region, and service mesh golden signals (latency, traffic, errors, saturation)."},
    {from:"large",to:"hyper",trigger:"Latency SLO <50ms globally, or DNS-based failover is too slow (>60s), or you need sub-second failover across regions",action:"Step 1: Implement anycast routing. If using Cloudflare Enterprise or Fastly, this is managed for you. If building custom: obtain your own /24 IPv4 block (from ARIN/RIPE), announce it via BGP from each PoP using a BGP daemon (BIRD or FRRouting), with health-aware route withdrawal (if a PoP fails, it stops announcing the route and traffic automatically reroutes to the next-nearest PoP in <30 seconds via BGP convergence). Step 2: Deploy per-PoP compute — either Cloudflare Workers (managed) or your own edge servers running Envoy + your application in containers. Step 3: Implement QUIC between PoPs for your internal backbone (avoids TCP head-of-line blocking even on your internal network). Step 4: Build a global configuration distribution system that pushes config changes (routing rules, rate limits, feature flags) to all PoPs within seconds (Cloudflare uses a custom system called Quicksilver that propagates config globally in <5 seconds). Step 5: Hire or develop network engineering expertise — at this level, you're operating internet infrastructure, not just application infrastructure. Organizations at this tier: Cloudflare, Fastly, Akamai, Google, Meta, Netflix, major gaming companies, and financial exchanges."}
  ],
  defined_terms:{
    "PoP":"Point of Presence — a physical location (usually a data center or colocation facility) where CDN/edge servers are deployed close to end users. Each PoP serves users in its geographic area.",
    "Anycast":"A routing technique where the same IP address is announced from multiple locations via BGP. The internet's routing infrastructure automatically directs each user's packets to the nearest (in network terms) announcing location.",
    "BGP":"Border Gateway Protocol — the routing protocol that holds the internet together. Every ISP and major network uses BGP to announce which IP address ranges they can reach. A BGP misconfiguration can redirect traffic globally.",
    "mTLS":"Mutual TLS — both the client and server present certificates and verify each other's identity. In a standard TLS connection, only the server proves its identity; in mTLS, both sides do. Essential for zero-trust service-to-service communication.",
    "Head-of-line blocking":"A TCP limitation where a single lost packet blocks delivery of all subsequent data on that connection, even data for unrelated requests multiplexed on the same connection. HTTP/3 (QUIC) solves this by isolating streams.",
    "Service mesh":"An infrastructure layer (typically implemented as sidecar proxies like Envoy attached to each service) that handles service-to-service networking concerns: mTLS, load balancing, circuit breaking, retries, and observability — without application code changes."
  },
  sizes_cfg:{
    small:{range:"Single region, simple DNS, <1K RPS",rec:"The minimum viable networking stack that every application should have from day one: (1) Domain registered and DNS managed by Cloudflare (free tier) — this gives you automatic CDN for static assets, HTTP/2, DDoS protection, and analytics. (2) HTTPS everywhere via Let's Encrypt (free, automated certificate issuance and renewal — Caddy does this automatically, or use certbot with Nginx). Never serve anything over HTTP. (3) HTTP/2 enabled on your reverse proxy (Nginx, Caddy, or Traefik). (4) Static assets (JS, CSS, images, fonts) served via CDN with long cache TTLs and content-hash filenames for cache busting. This stack costs $0/month and takes 1–2 hours to set up. Technology comparison: Cloudflare (free CDN + DNS + DDoS, the best value in infrastructure) vs CloudFront (tighter AWS integration, pay-per-use at ~$0.085/GB, no free tier for meaningful usage). Caddy (automatic HTTPS, zero-config HTTP/2, written in Go) vs Nginx (more manual config but more mature and widely documented) vs Traefik (excellent for container/Kubernetes environments with auto-discovery). For DNS: Cloudflare DNS (free, fastest public authoritative DNS) vs Route53 ($0.50/zone/month + $0.40/M queries, tighter AWS integration).",tools:["Cloudflare free","Let's Encrypt","Caddy or Nginx","AWS Route53"]},
    medium:{range:"CDN + GeoDNS, 1K–50K RPS, users on 2–3 continents",rec:"Upgrade the CDN to handle dynamic content caching: CloudFront or Cloudflare Pro ($20/month) can cache API responses with short TTLs (5–60s), reducing origin load by 30–60% for read-heavy APIs. Deploy edge workers for lightweight logic at the edge: Cloudflare Workers ($5/month for 10M requests) can handle A/B test assignment, geolocation-based redirects, authentication token validation, and request header manipulation — all at the edge in <1ms, avoiding a 100–200ms round-trip to origin. Enable TCP BBR congestion control on your servers (sysctl net.ipv4.tcp_congestion_control=bbr) — Google's BBR algorithm achieves 2–25% better throughput than the default Cubic, especially on high-latency or lossy paths. Enforce HTTP/2 everywhere. If you use gRPC for internal services, you already have HTTP/2. Technology comparison: Cloudflare Workers (300+ PoPs, <1ms cold start, JavaScript/Rust/Wasm, $0.50/M requests) vs Lambda@Edge (400+ CloudFront PoPs, 5–40ms cold start, Node.js/Python, $0.60/M requests, 5s execution limit) vs Fastly Compute@Edge (smaller network but WebAssembly-based with no cold start, $0.50/M). Cloudflare Workers is the best default choice for most — largest network, fastest cold start, cheapest, and the Durable Objects primitive enables stateful edge applications. AWS Global Accelerator ($0.015/hr + $0.01/GB) is an alternative to GeoDNS — it uses anycast to route traffic to the nearest AWS region, providing consistent latency improvement and faster failover than DNS-based routing.",tools:["CloudFront or Cloudflare Pro","Cloudflare Workers","AWS Global Accelerator","GeoDNS via Route53 or Cloudflare LB"]},
    large:{range:"Multi-region + service mesh, 50K–500K RPS, latency-sensitive globally distributed users",rec:"At this scale, three major networking investments become essential. (1) Service mesh: deploy Istio or Linkerd to handle all service-to-service networking. This gives you: mTLS between all services (zero-trust internally), distributed tracing headers injected automatically, per-service traffic metrics (latency, error rate, request volume) without code changes, traffic splitting for canary deploys, and circuit breaking / retry policies configured declaratively. Technology comparison: Istio (most feature-rich, largest community, but heaviest — control plane uses 1–2GB RAM, sidecar adds ~50MB per pod and 0.5–2ms latency per hop) vs Linkerd (lighter — control plane ~250MB, sidecar ~20MB, ~0.5ms added latency — simpler to operate but fewer advanced features) vs Cilium (eBPF-based — no sidecar proxy, operates at the kernel level for near-zero overhead, but newer and requires Linux kernel 5.4+). Recommendation: Linkerd for most organizations (90% of the value at 30% of the complexity), Istio only if you need advanced traffic management (fault injection, request mirroring, complex routing rules), Cilium if you're on modern kernels and want minimal overhead. (2) Global load balancing: Route53 latency-based routing or Cloudflare Load Balancing ($5/month per origin) to route users to the nearest healthy region. (3) HTTP/3 + QUIC for mobile clients — enable on your edge/CDN (Cloudflare enables by default, CloudFront supports it as a toggle). For mobile-heavy applications, HTTP/3 alone can reduce page load times by 10–30% on cellular networks. Additionally: implement request tracing end-to-end. Propagate the W3C 'traceparent' header from edge through every service to database. This lets you trace a single user request through your entire infrastructure — invaluable for debugging latency.",tools:["Istio or Linkerd","Cilium (eBPF mesh)","AWS Global Accelerator","Cloudflare","QUIC/HTTP3","Envoy proxy"]},
    hyper:{range:"Anycast + custom PoP network, >500K RPS, <50ms global latency SLO",rec:"At hyper scale, networking IS the product — you're operating internet infrastructure. (1) Anycast IP with BGP: announce the same IP prefix from every PoP via BGP. User packets are automatically routed to the nearest PoP by the internet's routing infrastructure. Failover is automatic: when a PoP goes down, it stops announcing the route and traffic converges to the next-nearest PoP within 5–30 seconds — no DNS, no application changes. This is how Cloudflare, Google, and Fastly operate. (2) 200+ PoPs globally: each PoP runs a complete edge stack — TLS termination, CDN cache, edge compute (Workers-style), WAF, rate limiting. The goal: sub-10ms first-byte time from any internet-connected user. (3) Custom CDN logic optimized for your content type: video streaming companies (Netflix) optimize for large sequential reads with predictive prefetch; API companies (Stripe) optimize for small, dynamic, uncacheable responses with connection pooling; gaming companies optimize for UDP-based protocols with minimal buffering. (4) QUIC internally between PoPs: even your internal backbone benefits from QUIC's 0-RTT connection resumption and stream isolation. (5) Custom congestion control: Google uses BBR v2 on their internal network; Akamai and Cloudflare have proprietary congestion algorithms tuned for their traffic profiles. (6) Dedicated fiber where economically justified: at sufficient traffic volume, leasing dark fiber between major PoPs is cheaper and faster than transiting the public internet. Technology comparison at this tier: Cloudflare Enterprise (managed, 300+ PoPs, Workers + Durable Objects, R2 storage, ~$5K+/month) vs Fastly (managed, ~90 PoPs, Compute@Edge + VCL, real-time logging, ~$500+/month) vs Akamai (largest legacy network, 4000+ PoPs, complex configuration, expensive, used by major media/financial companies) vs custom BGP (full control, requires dedicated network engineering team, BIRD/FRRouting for BGP, Envoy for edge proxy). Most companies should use Cloudflare or Fastly until their scale or customization needs truly require a custom network — that threshold is roughly >1M RPS sustained or highly specialized protocol requirements (gaming, financial trading).",tools:["Custom BGP anycast (BIRD/FRRouting)","Fastly Compute@Edge","Cloudflare Enterprise","custom QUIC (quic-go/msquic)","dedicated fiber","Akamai"]}
  },
  defined_terms_cfg:{
    small:["DNS","CDN","TLS","HTTP/2"],
    medium:["Edge worker","GeoDNS","BBR congestion control"],
    large:["Service mesh","mTLS","QUIC","distributed tracing"],
    hyper:["Anycast","BGP","PoP","dark fiber","congestion control algorithm"]
  },
  children:[]
};
