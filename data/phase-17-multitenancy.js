
const PHASE_MULTITENANCY = {
  name: "Phase 17 · Multi-Tenancy",
  id: "tenant",
  icon: "🏢",
  phase: "Multi-tenancy",
  color: "#06b6d4",
  sizes: ["small", "medium", "large", "hyper"],
  short: "Pooled → siloed → cell-per-tier · noisy-neighbor isolation — the architecture of trust between you and every customer simultaneously",
  keyInsight: "Multi-tenancy is the defining architectural challenge of SaaS. When you serve 1,000 customers from shared infrastructure, every architectural decision has a multiplier effect: a bug doesn't affect one customer, it affects all of them. A slow query from one customer doesn't degrade their experience — it degrades everyone's. A security flaw doesn't leak one customer's data — it potentially leaks all of it. The fundamental tension: isolation costs money (running separate infrastructure per customer is 10–100× more expensive than sharing), but sharing creates risk (data leaks between tenants, performance interference, blast radius of failures). The mature approach is tiered isolation: free/starter tenants share everything (cheapest), professional tenants get logical isolation (separate schemas, per-tenant rate limits), enterprise tenants get physical isolation (dedicated databases, dedicated compute). This maps your infrastructure cost to the revenue each customer generates — which is the only sustainable model.",
  mentalModel: "Think of multi-tenancy like an apartment building. Pooled tenancy is a dormitory: everyone shares the kitchen, bathroom, and living room. It's cheap ($0.001/tenant/month for infrastructure), but one person's loud music keeps everyone awake (noisy neighbor), and if someone leaves the front door open, everyone's stuff is at risk (security). Schema-per-tenant is like separate apartments with shared utilities: each tenant has their own space (schema), but they share the building's plumbing and electrical (same DB server). DB-per-tenant is like separate houses on the same street: fully isolated living spaces, but shared road/sewage infrastructure. Cell architecture is like gated communities in different neighborhoods: completely independent everything, connected only by shared city services. Each level up costs 10–100× more but provides proportionally more isolation. The key insight: your free users don't need (or deserve, economically) their own house. Your $100K/year enterprise customers absolutely do.",
  detail: {
    what: "Multi-tenancy is the architectural pattern where a single instance of software serves multiple customers ('tenants') simultaneously, with each customer's data, performance, and failures isolated from the others. It's the foundation of every SaaS business: Slack, Salesforce, Notion, Figma, and every other cloud product you use serves thousands to millions of organizations from shared infrastructure. The tenancy model operates at four levels: (1) Data isolation — ensuring tenant A cannot access tenant B's data, whether through application bugs, SQL injection, API manipulation, or infrastructure misconfiguration. This is the table-stakes requirement: a data leak between tenants is an existential security event. (2) Performance isolation — ensuring tenant A's heavy workload (a bulk import, a complex report, a runaway query) doesn't degrade tenant B's experience. This is the 'noisy neighbor' problem and the #1 operational complaint in shared SaaS. (3) Failure isolation — ensuring a bug triggered by tenant A's data (edge case, corrupt input, schema violation) doesn't crash the service for tenant B. This is blast radius containment. (4) Compliance isolation — ensuring that tenant A's data residency requirements (GDPR mandates EU storage) don't conflict with tenant B's (US government requires US-only storage). This is increasingly required for enterprise sales. The tenancy model you choose affects every layer of the stack: database design (shared tables vs. separate schemas vs. separate databases), caching (shared Redis vs. namespaced keys vs. separate instances), queuing (shared queues vs. per-tenant queues), compute (shared processes vs. per-tenant containers), and deployment (shared release vs. per-tenant versions).",
    why: "Getting multi-tenancy wrong manifests in three catastrophic ways. First, the data leak: a missing WHERE tenant_id = ? clause in one query, and tenant A can see tenant B's data. This is the most common SaaS bug — every SaaS company has had at least one close call. At Salesforce scale, this is front-page news. Second, the noisy neighbor: your biggest customer runs a monthly report that does a full table scan across 50M rows. For 30 minutes, every other customer's queries are slow because the database is saturated. Your 99.9% SLO is violated for 1,000 customers because of 1 customer's query. Third, the blast radius explosion: a new feature has a bug that only triggers on a specific data pattern. One tenant has that pattern. The bug causes a crash loop. Because all tenants share the same process, the crash loop takes down all tenants. In a siloed architecture, only that one tenant would be affected. The cost of tenancy decisions is also economic: pooled infrastructure costs ~$0.001/tenant/month at 10K tenants — extraordinarily efficient. DB-per-tenant costs ~$50+/tenant/month (managed Postgres/RDS minimum). Cell architecture costs ~$500+/tenant/month. If your average customer pays $29/month, you cannot afford DB-per-tenant (the infrastructure alone would cost more than the revenue). If your average enterprise customer pays $50K/year, DB-per-tenant at $50/month is a rounding error. Match isolation investment to customer value.",
    numbers: "Infrastructure cost per tenant by model (rough, at 1K–10K tenants): Pooled (shared everything, tenant_id column) = $0.001–0.01/tenant/month. Schema-per-tenant (same DB instance, separate schemas) = $0.05–0.50/tenant/month (DB overhead per schema is minimal but migrations scale linearly). DB-per-tenant (separate managed database) = $30–100/tenant/month (RDS minimum instance ~$30/month). Dedicated compute per tenant (separate containers/VMs) = $100–500/tenant/month. Cell architecture (full stack copy serving N tenants) = $500–5K/cell/month ÷ tenants per cell. Economic crossover: the point where a tenant should be siloed is when their annual contract value exceeds the annual cost of isolation by at least 5×. At $30/month DB-per-tenant cost ($360/year), silo tenants paying >$1,800/year ($150+/month). At $500/month cell cost ($6K/year), silo tenants paying >$30K/year. Common SaaS tier mapping: Free/Starter ($0–29/month) = pooled. Professional ($30–299/month) = schema-per-tenant or dedicated namespace. Enterprise ($300+/month or $10K+/year) = DB-per-tenant or dedicated cell."
  },
  tradeoffs: [
    { axis: "Cost efficiency vs Isolation", left: "Pooled: cheapest, all risk shared — one tenant's problem is everyone's problem", right: "Siloed: most expensive, total isolation — one tenant's problem stays their problem", pos: 0.5 },
    { axis: "Operational simplicity vs Per-tenant control", left: "Single deploy for all tenants: simple CI/CD, one version to maintain, consistent experience", right: "Per-tenant deployment: each tenant can be on a different version, different config — 10× more ops complexity", pos: 0.5 },
    { axis: "Schema flexibility vs Migration speed", left: "Shared schema: one migration updates all tenants instantly — but no per-tenant customization", right: "Schema-per-tenant: each tenant can have custom fields/tables — but migrations must run 10K times", pos: 0.5 }
  ],
  pitfalls: [
    { name: "The missing WHERE clause", desc: "The single most common SaaS security bug: a query forgets to filter by tenant_id. In a pooled model, this returns data from ALL tenants. It's not a matter of if this happens — it's when. Mitigations: (1) PostgreSQL Row Level Security (RLS) — the database itself enforces tenant isolation regardless of application code: `CREATE POLICY tenant_isolation ON orders USING (tenant_id = current_setting('app.current_tenant_id'));`. Even if application code forgets the WHERE clause, the database filters automatically. (2) ORM-level scoping: libraries like ActsAsTenant (Rails), django-tenants (Python), or custom middleware that adds tenant filtering to every query. (3) Code review gate: any raw SQL query without tenant_id in the WHERE clause fails CI. (4) Automated testing: integration tests that create data for tenant A, switch context to tenant B, and assert zero visibility." },
    { name: "Noisy neighbor kills your SLO for everyone", desc: "One enterprise customer runs a bulk export (SELECT * FROM events WHERE tenant_id = 42 — 50M rows). This saturates DB I/O for 15 minutes. Your 1,000 other tenants experience degraded performance. Their SLOs are violated because of one customer. Solutions: per-tenant query timeouts (statement_timeout = 5s for free tier, 30s for enterprise), per-tenant connection pool limits (PgBouncer per-pool configuration: free tenants share 5 connections, enterprise gets 20), read replica routing for heavy queries, and dedicated compute (enterprise's bulk export runs against their own replica)." },
    { name: "Schema migration at 10K schemas", desc: "You have 10,000 tenants with schema-per-tenant. A schema migration (adding a column) must run 10,000 times. Each takes 100ms. Total: 17 minutes of sequential schema changes, each potentially locking tables. If one fails, what happens to the other 9,999? Solutions: (1) Parallelize with rate limiting: run 50 migrations concurrently, throttled. (2) Make migrations idempotent (IF NOT EXISTS). (3) Use online DDL (pg_repack, gh-ost) for large tables. (4) Async migration: tenant gets the new schema on their next request (lazy migration). (5) At true scale, this is a strong argument for pooled with RLS over schema-per-tenant." },
    { name: "Tenant-specific customization spiraling into N codepaths", desc: "Enterprise customer A wants a custom field on invoices. Customer B wants a different approval workflow. Customer C wants a white-label domain. Before you know it, you have 50 if(tenant == X) branches in your code. This is unsustainable. Solutions: feature flags (per-tenant configuration, not per-tenant code), custom fields (JSON column or EAV pattern for tenant-specific data), webhook integrations (let tenants customize behavior externally), and configurable workflows (rules engine like n8n or custom state machines)." }
  ],
  examples: [
    { name: "Salesforce pods — cell architecture at the largest scale", desc: "Salesforce operates ~100 'pods' globally, each hosting ~10,000 customer organizations ('orgs'). Each pod is a complete, independent copy of the Salesforce stack: dedicated database cluster, application servers, cache tier, and job processing. An outage in one pod affects only the ~10K orgs in that pod — roughly 0.05% of Salesforce's customer base. Tenant onboarding routes new orgs to the least-loaded pod in their geographic region. This architecture has been Salesforce's secret weapon for 20+ years: it provides enterprise-grade isolation at SaaS economics. The tradeoff: rolling out a new feature requires deploying to all ~100 pods, which Salesforce does in waves (canary pods first, then broader rollout over days). This means not all customers are on the same version at the same time — a complexity they manage with feature flags." },
    { name: "Slack shards — per-workspace isolation", desc: "Each Slack workspace is pinned to a 'shard' — a MySQL + Redis + Memcached + worker cluster. One shard serves ~10K workspaces. If a shard has a problem, only those ~10K workspaces are affected. Slack can move a workspace between shards for load balancing or isolation (e.g., moving a noisy large enterprise to a dedicated shard). This is effectively a cell architecture with per-tenant routing at the workspace level. The migration process (moving a workspace between shards) involves dual-writing to both shards during the transition and then atomically switching the routing entry. It takes minutes with zero user-visible downtime." },
    { name: "Notion's tenant_id enforcement", desc: "Notion processes billions of block operations daily across millions of workspaces. They use a pooled model (shared Postgres, partitioned by workspace_id) with application-level tenant isolation. After an early scare with a cross-tenant data visibility bug (quickly caught and patched), they implemented defense-in-depth: ORM-level mandatory workspace scoping, database-level RLS as a backstop, and automated testing that specifically attempts cross-tenant access in CI." }
  ],
  levelUp: [
    { from: "small", to: "medium", trigger: "First enterprise or regulated customer, or the first noisy-neighbor incident where one customer's bulk operation slowed everyone down", action: "Add tenant_id as a foundational architectural element. Here's the concrete implementation: (1) Add tenant_id column to EVERY table (no exceptions). Use a UUID, not an auto-increment integer (auto-increment leaks information about your total customer count and is predictable). (2) Enable PostgreSQL Row Level Security (RLS): for each table, `ALTER TABLE orders ENABLE ROW LEVEL SECURITY;` then `CREATE POLICY tenant_isolation ON orders USING (tenant_id = current_setting('app.current_tenant_id')::uuid);`. In your application middleware, set the tenant context on every request: `SET app.current_tenant_id = '<tenant_uuid>';`. Now even if your application code forgets a WHERE clause, the database prevents cross-tenant access. (3) Add per-tenant rate limits at the API gateway: free tenants get 100 req/min, paid tenants get 1,000 req/min, enterprise gets 10,000. Use your API gateway's rate limiting feature (Kong, AWS API Gateway) with tenant_id as the rate limit key. (4) Add per-tenant query timeouts: in PgBouncer, configure per-pool settings so free tenants have 5s statement_timeout and enterprise has 30s. (5) Add tenant_id to all log entries and metrics labels (but be aware of cardinality — use tenant_id in logs/traces, not in Prometheus labels if you have >1K tenants)." },
    { from: "medium", to: "large", trigger: ">100 enterprise tenants or first compliance audit requiring demonstrable data isolation (SOC2 Type 2, HIPAA)", action: "Implement tier-based isolation. (1) Create tenant pools by tier: free/starter tenants share Pool A (one DB, RLS isolation, shared cache namespace, shared job queue). Pro tenants share Pool B (separate smaller pool with better per-tenant quotas, possibly separate schema for customization). Enterprise tenants get Pool C or dedicated resources (DB-per-tenant using Citus's per-tenant partitioning or a separate RDS instance). (2) Build a tenant routing layer: a service that maps tenant_id → {db_connection_string, cache_prefix, queue_name, feature_flags}. Every request goes through this router first. Store the mapping in a fast lookup (Redis or in-memory with 1-minute cache). (3) Schema-per-tenant for top tier: use Postgres schemas (CREATE SCHEMA tenant_42; CREATE TABLE tenant_42.invoices ...) for enterprise customers who need custom fields or compliance isolation. Libraries: django-tenants (Python), apartment (Rails). (4) Per-tenant observability: tag all metrics and logs with tenant_id and tier. Build a dashboard showing per-tenant: RPS, latency p99, error rate, storage used. This is essential for identifying noisy neighbors and for enterprise customer success conversations. (5) Tenant migration playbook: document and automate the process of moving a tenant from pooled to siloed (extract their data, provision dedicated resources, update routing, verify, cutover). This is the most important playbook you'll build — enterprise sales depends on it." },
    { from: "large", to: "hyper", trigger: ">1,000 large tenants or geo data residency requirements across jurisdictions (EU tenant data must be in EU, US tenant data in US)", action: "Deploy cell architecture. (1) Design a cell: each cell is a complete, independent copy of your entire stack — database cluster, cache tier, application servers, job workers, search index. Cells share nothing except a global routing layer and identity/auth service (which itself should be multi-region replicated). (2) Size cells: each cell hosts 1,000–10,000 tenants, handling 1–10% of total traffic. Provision N+2 cells (if you need 10 cells for capacity, deploy 12 — so losing 2 cells doesn't cause capacity problems). (3) Build the cell router: a lightweight, globally deployed routing service that maps tenant_id → cell_id → cell_endpoint. This service must be the most available component in your entire system (it's the gateway for everything). Deploy it at every PoP with Cloudflare Workers or Lambda@Edge. (4) Implement tenant placement: new tenants are assigned to the least-loaded cell in their geographic region. Placement considers: region (for data residency), current cell load, tenant tier (enterprise tenants on cells with lower density), and anti-affinity (competitors shouldn't be on the same cell if possible). (5) Build tenant migration between cells: the ability to move a tenant from Cell A to Cell B without downtime. Pattern: dual-write to both cells → backfill historical data to Cell B → atomic routing switch → cleanup Cell A. This is the hardest engineering in the entire system and the most critical — you'll need it for load balancing, compliance, and incident response. (6) Per-region data residency: cells have geographic affinity. EU cells in eu-west-1, US cells in us-east-1. The routing layer ensures EU tenants are always routed to EU cells. Cross-region access only for global admin operations, with explicit audit logging." }
  ],
  children: [
    {
      name: "Isolation Model",
      id: "tenant-iso",
      icon: "🚧",
      phase: "Multi-tenancy",
      color: "#06b6d4",
      sizes: ["small", "medium", "large", "hyper"],
      short: "Shared row → shared schema → DB-per-tenant → cell — each level adds isolation, each costs more",
      keyInsight: "The isolation model is a spectrum, not a binary choice, and the right answer is usually different for different tiers of customer. Your free users share everything (rows in the same table, filtered by tenant_id). Your $500/month customers get logical isolation (separate schema, per-tenant rate limits). Your $50K/year enterprise customers get physical isolation (dedicated database, dedicated compute). This tiered approach matches your infrastructure investment to the revenue each customer generates — and it matches your isolation investment to the trust and compliance requirements each customer demands.",
      detail: {
        what: "The isolation model defines how deeply one tenant's resources are separated from another's — both logically (can tenant A's code path access tenant B's data?) and physically (does tenant A's workload share CPU, memory, disk, and network with tenant B?). The spectrum from least to most isolated: (1) Shared row (pooled): all tenants' data lives in the same tables, distinguished by a tenant_id column. Application code (or RLS policies) filter queries. The database itself doesn't know tenants exist — it's entirely an application-level concept. Cheapest, most operationally simple, highest risk. (2) Shared schema, separate namespace: all tenants share the same database instance and schema, but use per-tenant prefixes or namespaces in caches, queues, and object storage. A step up from pure pooled because cache eviction and queue processing can be tenant-aware. (3) Schema-per-tenant: each tenant gets a separate database schema (Postgres: CREATE SCHEMA tenant_42). Tables are structurally identical but physically separate — a query in tenant_42's schema literally cannot see tenant_43's tables without explicit cross-schema reference. Same DB instance, so costs scale with DB capacity not tenant count. (4) DB-per-tenant: each tenant gets a separate database instance. Complete physical data isolation. The most operationally expensive pooled model (provisioning, backups, migrations happen N times), but provides compliance-grade isolation that auditors love. (5) Cell architecture: each cell is a fully independent stack (DB + compute + cache + queues) serving a group of tenants. Failure in one cell is invisible to tenants in other cells. The Salesforce/Slack model.",
        why: "The choice reverberates through every layer of your system. In a pooled model, adding a column is one migration that runs in seconds. In schema-per-tenant with 10K tenants, it's 10K migrations that run in minutes (and if one fails, you have a partially migrated fleet). In DB-per-tenant, it's 10K separate migration jobs, each hitting a different database. In a pooled model, a database backup captures all tenants in one snapshot. In DB-per-tenant, you have 10K separate backup jobs. Connection pooling: pooled uses one connection pool; DB-per-tenant needs per-tenant pools (PgBouncer can handle this, but the configuration complexity is real). Monitoring: pooled means one set of DB metrics; DB-per-tenant means 10K sets of metrics (you need aggregate views). The operational cost difference is not 10% — it's 10×. The isolation benefit is also not 10% — it's qualitative: auditors for SOC2, HIPAA, and FedRAMP strongly prefer demonstrable physical isolation, and some enterprise contracts explicitly require it.",
        numbers: "Cost per tenant by model at 1,000 tenants (rough): Pooled (tenant_id column): ~$0.001/tenant/mo (single RDS instance at $100/mo shared across 1K tenants, plus proportional compute/cache). Schema-per-tenant: ~$0.10/tenant/mo (same DB instance, slightly more memory per schema, migrations run N times). DB-per-tenant (RDS per tenant): ~$50/tenant/mo minimum (db.t3.micro is ~$15/mo + storage, but realistically $30–50 after backups and monitoring). Cell (full stack per 500 tenants): ~$1/tenant/mo ($500/cell/mo ÷ 500 tenants — but you need N+2 cells for redundancy). Match to ARR: if a tenant pays $10/month, spending $50/month on their dedicated DB is 5× their revenue — unsustainable. If they pay $5K/month, $50/month for dedicated DB is 1% of their revenue — no-brainer."
      },
      tradeoffs: [{ axis: "Density vs Blast radius", left: "All tenants in one DB: cheapest, but one bad query or one bug affects everyone", right: "DB per tenant: most expensive, but each tenant's issues are completely contained", pos: 0.5 }],
      sizes_cfg: {
        small: {
          range: "Pooled (shared everything) — the only sensible choice for early-stage SaaS",
          rec: "Every table gets a tenant_id column, no exceptions. Choose your enforcement mechanism: (A) PostgreSQL Row Level Security (RLS): the database itself prevents cross-tenant access. Setup: `ALTER TABLE tablename ENABLE ROW LEVEL SECURITY; CREATE POLICY tenant_pol ON tablename USING (tenant_id = current_setting('app.tenant_id')::uuid);`. In your app middleware: `SET LOCAL app.tenant_id = '<uuid>';` at the start of every request. Strength: bulletproof — even raw SQL can't bypass it. Weakness: slight performance overhead (~5%), debugging is harder when you forget to set the context. (B) ORM-level tenant scoping: libraries automatically add WHERE tenant_id = ? to every query. ActsAsTenant (Rails), django-tenants (Django), or custom middleware. Strength: simpler to debug, no DB-level configuration. Weakness: a single raw SQL query without scoping bypasses it entirely. Recommendation: use BOTH — ORM scoping as the primary mechanism (developer-friendly) and RLS as the safety net (catches anything the ORM misses). Cost at this model: effectively zero incremental cost per tenant beyond the proportional compute/storage.",
          tools: ["Postgres RLS", "tenant_id column pattern", "ActsAsTenant (Rails)", "django-tenants"]
        },
        medium: {
          range: "Pooled with schema-per-tenant for top-tier customers",
          rec: "Default tier: pooled with RLS (as above). Premium/enterprise tier: dedicated schema. Implementation: create a schema for each top-tier tenant (CREATE SCHEMA tenant_42; then CREATE TABLE tenant_42.orders (...);). Set search_path at connection level to route queries to the correct schema. The same DB instance serves both pooled and schema-isolated tenants — no new infrastructure. This gives you: (1) physical data separation within the same DB (tenant_42's tables are in a different schema namespace — a query in the default public schema cannot accidentally see tenant_42's data without explicit cross-schema reference); (2) per-tenant custom fields (add columns to tenant_42.orders without affecting other tenants); (3) easy migration path to full DB-per-tenant later (dump the schema, restore to a new DB). Technology comparison: Postgres schemas (native, zero cost, excellent isolation — the recommended approach). django-tenants (Python library that manages schema-per-tenant with automatic schema routing based on subdomain — battle-tested at thousands of tenants). apartment gem (Rails equivalent — same pattern). Citus (Postgres extension that provides per-tenant sharding transparently — distributes tenant data across worker nodes while maintaining a single connection endpoint; strength: automatic sharding, handles 100K+ tenants; weakness: query limitations on distributed tables, $$ for managed Citus on Azure).",
          tools: ["Postgres schemas", "apartment (Rails)", "django-tenants", "Citus"]
        },
        large: {
          range: "Tier-based pools + DB-per-tenant for enterprise customers",
          rec: "Three tiers of isolation mapped to three tiers of pricing: Tier 1 (Free/Starter, $0–49/month): pooled DB with RLS, shared cache with tenant-prefixed keys, shared job queue with per-tenant rate limits. Infrastructure cost: ~$0.01/tenant/month. Tier 2 (Professional, $50–499/month): separate pool (a dedicated DB cluster for pro tenants, each with their own schema). Better resource quotas: 20 connections vs 5 for free, 30s query timeout vs 5s. Infrastructure cost: ~$0.50/tenant/month. Tier 3 (Enterprise, $500+/month): dedicated DB instance (RDS or Aurora), dedicated Redis namespace or instance, dedicated worker pool. Can be in a specific region for compliance. Infrastructure cost: ~$50–200/tenant/month. The routing layer: a service (or middleware) that maps tenant_id → {tier, db_connection_string, cache_endpoint, queue_name}. Every inbound request hits this router first. Store the mapping in Redis (fast lookup) backed by a Postgres table (source of truth). Cache with 5-minute TTL. Tenant migration: the ability to upgrade a tenant from Tier 1 to Tier 3 (extract data from pooled DB, provision dedicated DB, migrate data, update routing, verify, cut over). Document and rehearse this process — enterprise sales velocity depends on how fast you can provision dedicated infrastructure. Technology: Citus (distributes tenant data across nodes automatically with distribute_table(orders, tenant_id) — each tenant's data stays together on one node; strength: transparent sharding, handles thousands of tenants at the DB level without application changes). ProxySQL (MySQL) or PgBouncer (Postgres): connection pooling with per-tenant routing rules. Vitess keyspaces: for MySQL-based architectures, each keyspace can serve a set of tenants.",
          tools: ["Citus per-tenant sharding", "ProxySQL", "PgBouncer per-pool", "Vitess keyspaces", "custom tenant router"]
        },
        hyper: {
          range: "Cell architecture — complete stack isolation per group of tenants",
          rec: "Each cell is an independent, identically-configured copy of your entire stack: its own database cluster (primary + replicas), its own Redis cluster, its own compute fleet (ECS/K8s namespace), its own job workers, and its own search index. Cells share only: (1) the global routing layer (maps tenant_id → cell_id → cell_endpoint), (2) the identity/authentication service (which is itself globally replicated), and (3) the billing/admin plane (for cross-cell management). Cell sizing: each cell serves 1,000–10,000 tenants, representing 1–5% of total capacity. Deploy N+2 cells so that losing 2 cells (total failure, not just degradation) still leaves sufficient capacity. Tenant placement algorithm: assign new tenants to the least-loaded cell in their geographic region, respecting anti-affinity constraints (competitors on different cells) and compliance constraints (EU tenants on EU cells only). Cell health monitoring: each cell reports its load, error rate, and capacity utilization to a central control plane. The control plane can: redirect new tenant signups away from unhealthy cells, trigger tenant migration off an overloaded cell, and initiate cell-level failover. Real-world examples: Salesforce 'pods' (~100 globally, ~10K orgs each), Slack 'shards' (MySQL+Redis+workers per shard, ~10K workspaces each), AWS itself (services deployed in cells per AZ — this is why AWS outages are often 'partial' — only one cell of the affected service is down). Technologies: AWS cell architecture pattern (documented by AWS for their own services), Terraform modules for reproducible cell provisioning (each cell is an identical Terraform workspace), Kubernetes namespaces for logical cell isolation within a shared cluster (simpler but less isolated than separate clusters), custom cell controller (a control plane service that manages cell health, capacity, and tenant routing).",
          tools: ["AWS cell architecture pattern", "Salesforce 'pods' model", "Slack 'shards' model", "Terraform workspaces per cell", "custom cell controller"]
        }
      },
      levelUp: [],
      pitfalls: [
        { name: "Forgetting tenant_id in one query — the SaaS original sin", desc: "A developer writes `SELECT * FROM invoices WHERE status = 'overdue'` — missing the tenant_id filter. In a pooled model, this returns overdue invoices for ALL tenants. If this powers an email notification, every tenant receives emails about other tenants' overdue invoices. This is a data breach. Defense in depth: (1) RLS at database level catches it even if application code fails. (2) ORM scoping catches it in normal code paths. (3) CI test: create records for 2 tenants, switch context, assert the other tenant's records are invisible. (4) Code review checklist: every SQL query must reference tenant_id. With all four layers, the probability of a leak approaches zero." },
        { name: "Noisy neighbor on pooled cache evicting everyone's data", desc: "One tenant's bulk import writes 100K records. Each write invalidates or adds cache entries. The Redis instance fills up; LRU eviction kicks in; it evicts OTHER tenants' hot cache keys. Suddenly, 500 tenants' cache hit rate drops from 95% to 40%, and your DB is slammed. Solutions: per-tenant cache namespaces with per-tenant memory quotas (Redis doesn't natively support this, but you can enforce it at the application level by tracking per-tenant key count), separate Redis instances per tier (free tenants share one Redis, enterprise gets dedicated), or cache-aside with short TTL (so evicted keys repopulate quickly)." },
        { name: "Migration hell at 10K schemas", desc: "Adding a column to 10K separate schemas takes hours of sequential DDL. If one schema has a locked table (long-running query), the migration hangs for that schema and blocks subsequent ones. Solutions: (1) Run migrations in parallel with concurrency limiter (e.g., 50 at a time). (2) Use online DDL tools (gh-ost for MySQL, pg_repack for Postgres) that don't lock tables. (3) Implement idempotent migrations (IF NOT EXISTS) so retrying is safe. (4) Async lazy migration: the migration runs per-tenant on their next request, triggered by a version check. (5) At true scale (>10K tenants), this operational burden is a strong argument for pooled-with-RLS over schema-per-tenant." }
      ],
      examples: [
        { name: "Salesforce pods", desc: "Each 'pod' hosts ~10K orgs with a full stack copy. ~100 pods globally. An outage on Pod NA44 (famous for issues) affects only the orgs on that pod. Salesforce publishes per-pod status (trust.salesforce.com) so customers know their pod's health independently. The pod architecture has been Salesforce's reliability backbone for 20+ years and is the most proven cell architecture in the industry." },
        { name: "Slack shards", desc: "Each Slack workspace is pinned to a shard (MySQL + Redis + Memcached + workers). Slack can move a workspace between shards for load balancing or to isolate a noisy enterprise customer. The migration is seamless: dual-write during transition, atomic routing switch, cleanup. This per-workspace sharding gives Slack cell-like isolation without full cell architecture overhead — a pragmatic middle ground." }
      ],
      related: [{ id: "shard-key", label: "Shard Key Design" }, { id: "reliability", label: "Reliability & DR" }],
      children: []
    },

    {
      name: "Noisy Neighbor",
      id: "tenant-noise",
      icon: "🔊",
      phase: "Multi-tenancy",
      color: "#06b6d4",
      sizes: ["medium", "large", "hyper"],
      short: "Per-tenant quotas, fair queueing, isolation — preventing one customer from ruining everyone else's day",
      keyInsight: "In pooled multi-tenant systems, resource consumption follows a power law: your top 1% of tenants consume 50%+ of resources (database queries, cache space, API calls, job queue slots). If you don't control for this, that top 1% determines the experience for the other 99%. Noisy neighbor mitigation is the art of giving heavy users their fair share without starving everyone else — and doing it without heavy users even noticing the limits until they're genuinely abusing shared resources.",
      detail: {
        what: "Noisy neighbor is the umbrella term for any situation where one tenant's resource consumption degrades other tenants' experience in a shared system. It manifests across every shared resource: (1) Database: one tenant's unindexed query does a full table scan, saturating disk I/O for minutes — all other tenants' queries slow down. (2) Cache: one tenant's bulk operation floods Redis with writes, evicting other tenants' cached data — their cache hit rate plummets. (3) API: one tenant's aggressive polling (1,000 requests/second from a buggy integration) consumes all API gateway capacity — other tenants get 429s or timeouts. (4) Job queue: one tenant enqueues 100K background jobs from a bulk import, filling the queue — other tenants' time-sensitive jobs (password reset emails, webhook deliveries) wait hours. (5) Compute: one tenant's memory leak or CPU-intensive operation on shared compute degrades co-located tenants' processes. The mitigation toolkit spans five approaches: (A) Rate limiting (per-tenant API quotas), (B) Resource quotas (per-tenant DB connections, cache memory, queue slots), (C) Fair queueing (weighted scheduling that gives each tenant a proportional share), (D) Throttling (reducing a noisy tenant's throughput dynamically when their usage exceeds thresholds), and (E) Physical isolation (moving the noisy tenant to dedicated resources — the nuclear option).",
        why: "Without noisy neighbor mitigation, your SaaS has a structural vulnerability: the experience of your median customer is determined by the behavior of your most extreme customer. One tenant doing a monthly 50M-row export tanks your database for 30 minutes — during which your other 1,000 tenants experience elevated latency, failed requests, and degraded trust. The insidious part: the noisy tenant might not even know they're causing problems (their export completed successfully!). Meanwhile, you've violated your SLO for 1,000 customers because of 1 customer's legitimate (but heavy) usage. The #1 complaint in B2B SaaS user forums is 'the platform is randomly slow' — and the root cause is almost always an uncontrolled noisy neighbor. Solving this is not just a reliability concern — it's a customer satisfaction and churn concern. A customer who experiences random slowdowns 3 times will start evaluating competitors, regardless of your feature set.",
        numbers: "Resource consumption distribution in typical SaaS: top 1% of tenants use 30–60% of DB queries, 40–70% of storage, and 20–50% of API calls. A single 99th-percentile tenant can consume more resources than the bottom 500 tenants combined. Quota design heuristic: set per-tenant limits at p99 × 3 of normal tenant usage — this allows heavy but legitimate usage while catching runaway or abusive patterns. For example: if the median tenant makes 100 API calls/minute and the p99 tenant makes 2,000/minute, set the limit at 6,000/minute. This catches buggy integrations and abuse while not affecting any normally-behaving tenant."
      },
      tradeoffs: [{ axis: "Strict quotas vs UX", left: "Tight limits: excellent isolation for the majority, but heavy users hit walls and complain — requires clear upgrade path (pay more for higher limits)", right: "Loose or no limits: best-effort performance, heavy users are happy but everyone else suffers during peaks — the 'tragedy of the commons'", pos: 0.5 }],
      howItWorks: {
        fair_queueing: "Instead of one FIFO queue for all tenants' background jobs, use weighted fair queueing (WFQ). Each tenant gets a virtual queue. The job scheduler round-robins between tenant queues, giving each tenant proportional time. If tenant A has 10,000 jobs and tenant B has 10, tenant B's jobs don't wait behind 10,000 of tenant A's. Implementation: Sidekiq (Ruby) supports per-queue priorities and weights. BullMQ (Node) supports rate limiting per queue. Custom: maintain per-tenant queues in Redis, pop from each in round-robin order with configurable weights (enterprise tenants get 3× the weight of free tenants).",
        adaptive_throttling: "Rather than hard rate limits (reject at threshold), use adaptive throttling: as a tenant's usage approaches their quota, gradually increase their request latency (add artificial delay). From the tenant's perspective, the system just gets a bit slower under heavy load — which feels natural and doesn't cause hard errors. Implementation: maintain a per-tenant 'load score' (requests in last minute / quota). When score > 0.8, add delay = (score - 0.8) × 500ms. At score 1.0 (quota reached), delay = 100ms per request. At score > 1.2, reject with 429. This gives heavy users a graceful degradation curve instead of a cliff."
      },
      sizes_cfg: {
        medium: {
          range: "Basic per-tenant rate limits and query timeouts",
          rec: "Three quick wins that prevent 80% of noisy neighbor issues: (1) Per-tenant API rate limits: implement in your API gateway (Kong rate-limiting plugin, AWS API Gateway per-key throttling, or application-middleware like rack-attack for Rails). Set limits per pricing tier: Free = 100 req/min, Pro = 1,000, Enterprise = 10,000. Include Retry-After header in 429 responses so clients know when to retry. (2) Per-tenant database query timeout: configure statement_timeout per connection (Postgres: SET statement_timeout = '5000';). In PgBouncer, configure per-pool timeouts — free tenant pool has 5s timeout, enterprise pool has 30s. This prevents one tenant's pathological query from holding connections indefinitely. (3) Per-tenant connection pool partitioning: in PgBouncer, create separate pools per tenant tier. Free tenants share a pool of 20 connections (across all free tenants). Enterprise tenants each get a dedicated pool of 10 connections. This prevents free-tier traffic spikes from exhausting connections that enterprise tenants need. Technology: Postgres statement_timeout (set per-session or per-transaction — the simplest and most effective DB-level protection). PgBouncer per-pool configuration (create named pools with different server-side settings). API gateway quotas (Kong, AWS API Gateway, or custom middleware — enforce at the edge before requests reach your app).",
          tools: ["Postgres statement_timeout", "PgBouncer per-pool", "API gateway quotas (Kong/AWS)", "rack-attack (Rails)"]
        },
        large: {
          range: "Fair queueing + tier-based SLOs + per-tenant observability",
          rec: "At this scale, basic rate limits aren't enough — you need comprehensive resource accounting. (1) Weighted fair queueing for background jobs: each tenant's jobs go into a per-tenant virtual queue. The job worker round-robins between tenants, giving each tenant proportional throughput regardless of how many jobs they've enqueued. Implementation in Sidekiq: separate queues per tenant tier with weight configuration (`queues: [enterprise,5] [pro,3] [free,1]`). In Kubernetes: separate job worker deployments per tier with different resource allocations. (2) Per-tenant CPU/memory quotas: in Kubernetes, use ResourceQuotas per namespace (if using namespace-per-tenant-tier) or LimitRanges per pod. For non-K8s: use cgroups v2 to limit CPU and memory per tenant's processes. (3) Tier-based SLO targets: explicitly different SLOs per tier — Free = 99% availability + p99 <1s, Pro = 99.9% + p99 <500ms, Enterprise = 99.99% + p99 <200ms. Publish these in your pricing page. This sets expectations and justifies the price difference. (4) Per-tenant observability: build a dashboard that shows, per tenant: current RPS, p99 latency, error rate, DB query count, cache hit rate, queue depth. Use this for three purposes: identifying noisy neighbors proactively (alert if any tenant exceeds 10× median resource usage), customer success conversations ('Tenant X, your usage pattern is approaching your tier limit — consider upgrading'), and incident response ('which tenant's workload changed to cause this degradation?'). (5) Adaptive throttling: as a tenant approaches their rate limit, gradually slow their requests rather than hard-rejecting. This provides a smoother UX than sudden 429 errors.",
          tools: ["Sidekiq weighted queues", "Kubernetes ResourceQuotas", "per-tenant Grafana dashboards", "custom adaptive throttler"]
        },
        hyper: {
          range: "Hardware-level isolation + predictive throttling",
          rec: "At hyper scale, you need enforcement that can't be circumvented by application-level bugs. (1) Per-tenant cgroups v2: every tenant's processes (web workers, job workers) run in a dedicated cgroup with hard CPU and memory limits. If a tenant's code has a memory leak, it OOMs within its cgroup — other tenants' processes are unaffected. This is hardware-enforced isolation at the OS level. (2) Dedicated nodes for top-tier tenants: Kubernetes nodeAffinity/taints ensure enterprise tenants' pods run on dedicated nodes that free-tier tenants can't be scheduled on. The enterprise nodes have guaranteed CPU and memory not shared with anyone. (3) eBPF-based per-tenant network QoS: use eBPF programs attached to network interfaces to enforce per-tenant bandwidth limits and packet prioritization. Enterprise tenants' packets get priority queuing; free tier gets best-effort. Cilium (K8s networking with eBPF) supports bandwidth management per pod. (4) Predictive throttling: use ML models trained on historical tenant usage patterns to predict noisy behavior before it happens. If tenant X typically does a monthly bulk export on the 1st at 3am, pre-emptively shift their workload to a dedicated read replica 30 minutes before. Monitor: tenant usage anomaly detection (Z-score > 3 from their own historical baseline triggers investigation). (5) Tenant isolation score: assign each tenant a dynamic 'isolation score' based on their tier, usage pattern, and contract requirements. The score determines their resource allocation, scheduling priority, and monitoring sensitivity. This creates a unified framework for all noisy-neighbor decisions.",
          tools: ["cgroups v2", "Kubernetes RuntimeClasses + node affinity", "eBPF/Cilium bandwidth management", "custom ML anomaly detection"]
        }
      },
      levelUp: [],
      related: [{ id: "api-rate", label: "Rate Limiting" }, { id: "compute-lb", label: "Load Balancing" }],
      children: []
    }
  ]
};
