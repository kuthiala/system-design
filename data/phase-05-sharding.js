const PHASE_SHARDING = {
  name:"Phase 5 · Sharding & Replication",id:"shard",icon:"🔀",phase:"Sharding",color:"#d97706",
  sizes:["small","medium","large","hyper"],
  short:"Key design · shard count · replication model · routing — the art of splitting data without splitting your sanity",
  detail:{
    what:"Sharding and replication are two related but distinct strategies for distributing data across multiple machines, and understanding the difference is critical. Sharding (also called horizontal partitioning) means splitting your dataset so each machine holds a different subset of the data — think of it like dividing a phone book into A–M and N–Z across two shelves. Each shelf (shard) is responsible for a portion of the total data, and you need a routing mechanism to know which shelf to check for a given name. Replication means copying the same data to multiple machines so that if one machine dies, the others still have the data — like keeping three identical copies of the entire phone book in three different buildings. Together, sharding gives you horizontal scale (more total data and throughput than one machine can handle), and replication gives you durability and availability (data survives hardware failures). In practice, every production distributed database uses both: data is sharded across N groups, and each group is replicated R times. For example, a 3-shard, 3-replica setup has 9 total data copies (3 shards × 3 replicas each), where each shard holds ~33% of the data and each replica of that shard has the same ~33% for redundancy. These two mechanisms are the core of every distributed database — Cassandra, DynamoDB, CockroachDB, Vitess, MongoDB — and they're also the source of most of their complexity. When someone says 'distributed database,' they really mean 'sharded and replicated database with all the trade-offs that entails.'",
    why:"You shard because vertical scaling has a ceiling — the biggest machine money can buy (e.g., AWS's db.r6g.16xlarge with 64 vCPUs and 512GB RAM at ~$8,000/month) can only hold so much data and serve so many queries per second. When your PostgreSQL instance is maxed at 50K writes/sec on that hardware and you need 200K, no amount of money buys a bigger single machine. Sharding splits the load: 4 shards each handling 50K writes/sec gives you 200K total. You replicate because every hard drive eventually fails (annual failure rate ~2–5% for enterprise SSDs), every machine eventually crashes, and every AZ eventually has an outage. Without replicas, a single disk failure means downtime until you restore from backup (hours) and potential data loss (everything since the last backup). With 3 replicas, you survive 2 simultaneous failures with zero data loss and zero downtime — the remaining replica promotes to primary within seconds. The costs are real and substantial: sharding eliminates efficient cross-shard joins (a query that touches all shards is called a scatter-gather and is N× slower), complicates transactions (a transaction spanning two shards requires two-phase commit or saga patterns), and demands careful key design (a bad shard key creates hot spots that defeat the purpose of sharding). Replication introduces consistency questions (which copy is authoritative after a failure? What about writes that reached one replica but not another?), replication lag (replicas may be seconds behind the primary, causing stale reads), and operational overhead (monitoring lag, managing failover, handling split-brain). Get either wrong and you'll have hot spots (one shard receiving 80% of traffic), data loss (replica promoted before receiving latest writes), or both. This is why the universal advice is: don't shard until you've exhausted vertical scaling and read replicas. Vertical scaling is 3–5× cheaper in operational effort than horizontal scaling. Every tier of complexity you add should be forced by necessity, not anticipated by fear.",
    numbers:"Rule of thumb: don't shard until you've exhausted vertical scaling (biggest available instance type) AND read replicas (offload read traffic). Vertical scaling is typically 3–5× cheaper in total operational cost than horizontal sharding, because sharding brings an entire category of new problems (cross-shard queries, distributed transactions, resharding, routing) that vertical scaling avoids entirely. Key numbers: PostgreSQL single node ceiling ~50K writes/sec on tuned 16-core NVMe hardware, comfortable up to ~500GB–1TB before maintenance (VACUUM, reindexing) becomes painful. MySQL similar ceiling, maybe slightly lower. Cassandra: each node handles ~10K–30K writes/sec depending on row size; clusters scale linearly — a 30-node cluster handles 300K–900K writes/sec. DynamoDB: effectively unlimited if partition key is well-designed, but costs $0.25/million writes (expensive at hyperscale). Shard count: use powers of 2 (8, 16, 32, 64, 128, 256) because splitting a shard in half during resharding is much simpler than arbitrary redistribution. Over-provision shard count modestly: if you need 4 shards today, create 16 and assign 4 groups of 4 logical shards per physical node — when you need to scale, move logical shards to new physical nodes without data reshuffling. Replication factor: 3 is the industry standard sweet spot (primary + 2 replicas). It survives 1–2 simultaneous node failures, costs 3× the storage of a single copy, and provides enough replicas for read scaling. Factor of 5 is used for ultra-critical data (ZooKeeper default) but the extra 66% storage cost is rarely justified. RPO (Recovery Point Objective — how much data loss is tolerable): synchronous replication gives RPO=0 (zero data loss, at cost of write latency), asynchronous gives RPO of seconds to minutes (the replication lag window)."
  },
  principles:[
    {name:"Delay sharding as long as possible",desc:"Sharding is a one-way door — once you shard, every query, every migration, and every operational procedure becomes harder. Exhaust these options first, in order: (1) Query optimization — find and fix slow queries using EXPLAIN ANALYZE, add indexes, rewrite N+1 patterns. This alone often 2–5× your effective capacity. (2) Vertical scaling — move to a bigger instance (e.g., db.r6g.4xlarge → db.r6g.16xlarge). 2–4× capacity increase for a config change. (3) Read replicas — offload read traffic to 1–3 replicas. If you're 80% reads (most apps are), this 4× your read capacity. (4) Connection pooling (PgBouncer) — if you're hitting connection limits, pooling can 10× your effective connection count. (5) Caching (Redis) — if specific queries are hot, cache them and remove 90%+ of those reads from the DB entirely. (6) Partitioning (not sharding) — PostgreSQL table partitioning splits a large table into sub-tables within the same instance, improving query performance on time-series or range data without distributed system complexity. Only after ALL of these are exhausted should you shard."},
    {name:"Shard key is forever",desc:"Once data is written with a shard key, changing it requires reading every row, recomputing the shard assignment, and writing it to the new location — a full data migration that takes weeks to months for large datasets and carries data-loss risk. Treat shard key selection with the gravity of a database schema design that will live for 5+ years. Prototype your key with real query patterns before committing. Ask: 'What are my 5 most common queries? Does each one touch 1 shard (good) or all shards (bad)?' If more than 10% of your common queries need scatter-gather, your key is wrong."},
    {name:"Replication is not backup",desc:"Replicas protect against hardware failure — a dead disk or crashed server. They do NOT protect against logical errors — a bad deploy that corrupts data, an accidental DELETE without WHERE, or a bug that writes garbage. A replica faithfully replicates the corruption. You still need point-in-time backups (pg_basebackup, WAL archiving, RDS automated snapshots) that let you restore to a specific moment before the error. Think of replicas as protection against physical problems and backups as protection against human problems."},
    {name:"Test failover before you need it",desc:"The worst time to discover that your failover doesn't work is during an actual outage at 3am. Run failover drills quarterly: intentionally kill the primary, verify that promotion happens within your RTO target, verify that the application reconnects cleanly, verify that replication resumes when the old primary is restored. Document every step and every surprise. Companies like GitHub, Slack, and Stripe run these drills regularly — it's the only way to have confidence that your HA setup actually works."}
  ],
  tradeoffs:[{axis:"Operational simplicity vs Horizontal scale",left:"Single node: one backup, one failover, no routing, no cross-shard queries — but capped at hardware limits",right:"Sharded: unlimited horizontal scale — but cross-shard joins are expensive, transactions are complex, operational overhead is 5–10× higher",pos:0.5},{axis:"Strong consistency vs Availability (replication model)",left:"Raft/leader-based: every write goes to a single leader, replicas follow — simple consistency model, but leader is a bottleneck and failover takes seconds",right:"Leaderless quorum (Dynamo-style): any node accepts writes, consistency tuned via R+W>N — always available during partition, but conflict resolution is your problem",pos:0.5}],
  pitfalls:[
    {name:"Sharding too early — the premature optimization trap",desc:"You shard at 5K RPS because a blog post said 'always shard early.' Now every developer must understand shard routing, every query plan must account for shard locality, every test environment needs a multi-shard setup, every migration takes 4× longer, and your actual daily load is 1K RPS that a single Postgres instance handles without breaking a sweat. You've paid 5–10× more in operational complexity for capacity you don't use and might never need. The pragmatic approach: monitor your vertical ceiling (CPU %, disk I/O, connections, query latency) and plan the sharding migration when you're at 60% of that ceiling — not before. You'll have months of runway to execute the migration carefully. Instagram ran on a single Postgres instance far longer than anyone expected; Figma did the same. Vertical scaling goes further than the internet suggests."},
    {name:"Cross-shard transactions — the distributed system trap",desc:"Your shard key is user_id. A 'transfer money between users' operation touches two shards (sender and receiver). A true atomic transaction across shards requires two-phase commit (2PC): the coordinator asks both shards to prepare, then asks both to commit. If either shard fails between prepare and commit, the coordinator must wait (potentially forever if the coordinator crashes too). 2PC is slow (~2× single-shard latency), fragile (any participant timeout blocks everyone), and operationally painful. Alternatives: (1) Choose a shard key that keeps transactions local — if transfers always happen within an 'account group,' shard by account_group_id. (2) Saga pattern: debit sender (local transaction on shard A), then credit receiver (local transaction on shard B), with a compensating 'refund sender' action if the credit fails. Use Temporal for orchestration. (3) For idempotent operations, use at-least-once delivery with deduplication."},
    {name:"Async replication + read-your-writes failure — the confused user trap",desc:"User updates their profile name to 'Alice.' The write goes to the primary. They refresh the page. The read is routed to a replica that's 2 seconds behind. The page still shows 'Bob.' User refreshes again — 'Alice' now shows. User thinks the app is buggy. This is the #1 user-facing symptom of eventual consistency and it destroys trust. Solutions, in order of preference: (1) After any write, route that user's subsequent reads to the primary for N seconds (typically 5–10s) using a cookie or session flag — this gives read-your-writes semantics without requiring all reads to hit the primary. (2) Use RETURNING clause in PostgreSQL to return the written data directly in the write response, so the UI updates from the write response rather than a subsequent read. (3) Optimistic UI: update the client-side state immediately from the write action; don't re-fetch from server until enough time has passed for replication."},
    {name:"Resharding without a plan — the multi-month nightmare",desc:"You started with 4 shards. One shard is now hot (receiving 60% of writes because your shard key has a skewed distribution). You need to split it into 2 shards to rebalance. Without preparation, this means: (1) provisioning new hardware, (2) building a data-copy pipeline, (3) pausing writes (or implementing double-write) during cutover, (4) updating the shard map, (5) verifying data integrity, (6) cleaning up old data. This can take weeks to months and carries data-loss risk if done wrong. The prepared approach: (1) Use a sharding framework with built-in online resharding (Vitess, Citus, CockroachDB all support this). (2) Over-provision logical shards from the start (create 256 logical shards mapped to 4 physical nodes — resharding is just moving logical shards between physical nodes, which is a data-copy operation, not a data-reshard operation). (3) Use consistent hashing, which redistributes only ~1/N of data when adding a node, rather than rehashing everything."},
    {name:"Routing layer as single point of failure — defeating the purpose",desc:"All requests go through a single routing proxy to reach the right shard. The proxy crashes → total outage. You've replaced one SPOF (the database) with another (the router). Solutions: (1) Multiple proxy instances behind a load balancer (e.g., 3 Vitess vtgate instances behind an ALB). (2) Smart client: the application library holds the shard map locally and connects directly to the correct shard, eliminating the proxy entirely (Redis Cluster does this). (3) Gossip protocol: every node knows the topology; any node can route any request (Cassandra does this). The routing layer must be at least as available as the data layer it fronts."}
  ],
  examples:[
    {name:"Vitess at YouTube → Slack → the world",desc:"YouTube built Vitess in 2010 when their single-node MySQL couldn't keep up with the world's video comment traffic. Vitess is a sharding middleware for MySQL: it sits between your application and multiple MySQL instances, routing queries to the correct shard, handling cross-shard queries via scatter-gather, and providing online resharding without downtime. YouTube donated it to the CNCF; now Slack, HubSpot, Square, and many others use it. Vitess is the best option when you've outgrown single-node MySQL but want to keep MySQL's query language and tooling. The key Vitess concepts: VSchema (defines how tables map to shards), vtgate (the query router, stateless, horizontally scalable), vttablet (the per-shard agent managing the MySQL instance), and vtctl (the control plane for resharding operations). Strength: mature, battle-tested, supports online resharding. Weakness: MySQL-only, significant operational complexity, learning curve for VSchema design. If you're on PostgreSQL, the equivalent is Citus (acquired by Microsoft, available as Azure Cosmos DB for PostgreSQL) — it adds sharding to Postgres as an extension, supporting distributed queries, columnar storage, and online rebalancing."},
    {name:"Discord's bucketed time-series sharding — a masterclass in composite keys",desc:"Discord stores trillions of messages. Their shard key is a composite: (channel_id, time_bucket). Each shard holds messages for a set of channels within a specific time window. This design achieves two seemingly contradictory goals: (1) even write distribution — new messages spread across many shards because channels are diverse, and (2) query locality — reading recent messages in a channel touches exactly one shard (the current time bucket for that channel), making the most common query O(1) in shard fan-out. Historical queries (scrolling back in time) touch one shard per time bucket — predictable and bounded. The key insight: most chat queries are 'recent messages in channel X,' which maps perfectly to one shard. Rare queries like 'search all messages from user Y' require scatter-gather across all shards, but those are infrequent and can be served by a separate search index (Elasticsearch) rather than the primary shard layer. Discord originally used Cassandra for this, then migrated to ScyllaDB (Cassandra-compatible, written in C++) for ~3–10× better per-node performance at lower tail latency. The migration itself is a lesson: same data model, same shard key, different engine — the good shard key design made the migration possible by keeping the logical structure stable while swapping the physical engine."},
    {name:"Instagram's user_id sharding — simple and effective",desc:"Instagram sharded their PostgreSQL database by user_id very early in their history. Each user's photos, likes, comments, and followers live on the same shard. This means: reading a user's profile = single-shard query (fast). Posting a photo = single-shard write (fast). Viewing your own feed = multi-shard scatter-gather (slow, because feed includes posts from followed users on different shards) — but feeds are assembled asynchronously and cached in Redis, so the scatter-gather cost is paid in the background, not on the hot path. Cross-shard operations (like 'how many total users liked this photo from different shards') are handled via counters updated asynchronously. The lesson: optimize the shard key for your most common queries, and accept that rare or background queries will be slower. Instagram made the right call for their access pattern: user-centric queries are fast, global queries are deferred."}
  ],
  checklist:[
    "Have I exhausted vertical scaling, read replicas, connection pooling, caching, and table partitioning before sharding?",
    "Can I articulate my top 5 query patterns and verify each touches ≤1 shard with my proposed key?",
    "Is my shard key high-cardinality (at least 100× more distinct values than planned shard count)?",
    "Have I modeled the traffic distribution across shards — is each shard within ±20% of average?",
    "Do I have a plan for the 2–3 queries that WILL require cross-shard scatter-gather?",
    "Is my replication factor ≥3 for production data?",
    "Have I tested failover by killing the primary and measuring actual promotion time?",
    "Do I have a resharding plan (or am I using a framework like Vitess/Citus that provides one)?",
    "Is my routing layer redundant (no SPOF)?",
    "Am I monitoring replication lag with alerts when it exceeds my staleness budget?"
  ],
  levelUp:[],
  children:[
    {name:"Shard Key Design",id:"shard-key",icon:"🗝️",phase:"Sharding",color:"#d97706",
     sizes:["large","hyper"],short:"The single most important decision in horizontal sharding — choose wrong and you'll pay for years",
     detail:{
       what:"The shard key is a function — hash, range, or composite — that maps every record to a specific shard. It's the routing address for your data: given a record, the shard key tells you which shard owns it. A hash-based key (e.g., hash(user_id) % shard_count) spreads data uniformly but eliminates range queries. A range-based key (e.g., timestamp ranges — shard 1 holds Jan–Mar, shard 2 holds Apr–Jun) preserves ordering for range scans but creates hot spots on the 'current' range. A composite key (e.g., (tenant_id, time_bucket)) combines elements to balance both distribution and query locality. The shard key determines three things: (1) how evenly data and traffic are distributed across shards (write spread), (2) how many shards a typical query must touch (query affinity), and (3) how painful resharding will be (key stability). Once data is written with a shard key in production, changing the key requires a full data migration — reading every record, recomputing its shard assignment, and writing it to the new location. For large datasets (hundreds of millions of rows), this takes weeks to months. This is why shard key selection deserves more design time than almost any other decision.",
       why:"The shard key is the single most consequential decision in horizontal sharding because a bad key creates hot spots that completely negate the purpose of sharding. Imagine you have 16 shards and one receives 80% of all writes because your key has skewed distribution — you've added all the complexity of a distributed system (cross-shard queries, distributed transactions, routing, resharding) while getting only 20% of the theoretical benefit. The remaining 15 shards are mostly idle while one is melting. Conversely, a key that distributes writes perfectly but forces every read query to scatter-gather across all 16 shards turns every single-row lookup into a 16-way fan-out — adding latency proportional to the slowest shard (tail latency amplification). The ideal key balances two competing goals: spread writes evenly (each shard gets ~1/N of traffic) while keeping related reads on the same shard (most queries touch 1–2 shards, not all N). This is the core tension and the core puzzle of shard key design. The best keys achieve both via composite structure: one component provides cardinality for even distribution, another provides affinity for common query patterns.",
       numbers:"Good key health metrics: each shard within ±20% of average traffic — monitor this weekly and alert if any shard exceeds 2× average (hot spot forming). Cardinality: the shard key should have at least 10× more distinct values than your shard count. If you have 64 shards, you need at least 640 distinct key values (ideally much more). Low cardinality → some shards get many more keys than others (pigeonhole principle). Cross-shard query rate: keep below 10% of total queries. If more than 10% of your queries need scatter-gather, consider co-locating related data via composite keys or rethinking the key entirely. Scatter-gather overhead: a query fanning out to N shards has latency = max(shard_1_latency, shard_2_latency, ..., shard_N_latency) — tail latency amplification means your p99 degrades rapidly with fan-out degree. For 16 shards, if each shard has p99 of 20ms independently, the scatter-gather p99 is ~45ms (not 20ms). Each 2× increase in fan-out adds roughly 30–50% to tail latency."
     },
     keyPatterns:[
       {name:"Hash-based (e.g., hash(user_id) % N)",desc:"Distributes uniformly regardless of key distribution. Great for write spread. But: range queries are impossible (you can't find 'all users with IDs 1000–2000' without scatter-gather), and adding/removing shards requires rehashing (use consistent hashing to minimize redistribution to ~1/N of data). Best for: key-value lookups where you always know the exact key. Used by: DynamoDB (partition key hash), Redis Cluster, Cassandra (partition key). Technologies: consistent hashing (distributes keys on a virtual ring; adding a node moves only 1/N of keys), jump consistent hash (Google's algorithm, more uniform than ring-based, used in Envoy), rendezvous hashing (each key picks the node with highest hash score, good for distributed caches)."},
       {name:"Range-based (e.g., timestamp ranges, alphabetical)",desc:"Preserves ordering: queries like 'all events from 2024-01-01 to 2024-01-31' hit only the shards covering that range. But: hot spots on the 'active' range — if you shard by timestamp, all current writes go to the newest shard while older shards are idle. Workload is inherently unbalanced. Best for: time-series data WITH a secondary distribution component (composite key). Used by: HBase, Google Bigtable, CockroachDB (range-based sharding with automatic splitting). Anti-pattern: pure timestamp-only shard key — creates a write-hot 'tail' shard."},
       {name:"Composite (e.g., (tenant_id, time_bucket))",desc:"Combines two or more components to achieve both distribution AND affinity. Example: (channel_id, weekly_time_bucket). channel_id provides cardinality across many channels (even distribution), and time_bucket groups recent messages together (query affinity for 'recent messages in channel X'). This is the most sophisticated and usually the best approach. Discord's message sharding uses this pattern. The first component should be high-cardinality for distribution; the second provides locality for common queries. Design rule: the first component of a composite key should cover your most common query's WHERE clause."},
       {name:"Directory-based / lookup table",desc:"A central directory maps each key to its shard. Maximum flexibility: you can move any key to any shard at any time by updating the directory. But: the directory itself is a bottleneck and SPOF (mitigate with caching and replication). Best for: multi-tenant systems where you want per-tenant shard assignment (move a hot tenant to a dedicated shard). Used by: Vitess (VSchema as a form of directory), custom tenant routing tables. Performance: directory lookup adds ~1ms if cached, ~5ms if not."}
     ],
     tradeoffs:[{axis:"Query affinity vs Write uniformity",left:"Data co-located for queries: common reads are fast, single-shard — but popular entities become hot spots",right:"Data spread evenly for writes: uniform load distribution — but most queries need scatter-gather across shards",pos:0.5},{axis:"Range scans vs Even distribution",left:"Range key: efficient ordered scans and time-windowed queries — but write-hot tail on the newest range",right:"Hash key: perfectly even write distribution — but range queries require scatter-gather across all shards",pos:0.5}],
     pitfalls:[
       {name:"Monotonically increasing key (auto-increment ID, timestamp)",desc:"All new writes go to the single shard that owns the 'newest' range — the write-hot tail problem. The other shards serve only historical reads and are mostly idle. This completely defeats the purpose of sharding. Solutions: (1) Hash the monotonic value before routing (hash(auto_increment_id) % N distributes evenly). (2) Use a composite key with a high-cardinality entity ID as the first component: (user_id, timestamp) distributes writes across users while preserving time-ordering within each user. (3) For pure time-series, use a bucketed composite: (time_bucket_id, hash(source_id)) — ensures each time bucket's writes spread across sub-shards."},
       {name:"Sharding by tenant when one tenant is 1000× larger than average",desc:"99 tenants each produce 0.1% of traffic on 99 shards. 1 mega-tenant produces 90% of traffic on 1 shard. That shard is 90× hotter than the others. Your sharding achieved nothing. Solutions: (1) Sub-shard the mega-tenant using a composite key: (tenant_id, hash(entity_id) % K) where K is proportional to the tenant's traffic share. The mega-tenant gets K shards; small tenants share shards. (2) Use a directory-based approach that maps the mega-tenant to a dedicated shard group while small tenants share pools. (3) Quotas + load shedding on the mega-tenant to prevent it from dominating shared infrastructure."},
       {name:"Sharding by an enum-like low-cardinality field",desc:"You shard by 'status' which has 5 values: pending, active, completed, cancelled, archived. 5 possible shards, max. If 'active' represents 80% of rows, that shard has 80% of the data and traffic. Low cardinality keys create inherently unbalanced distributions. Rule: shard key cardinality should be at least 100× your shard count. If your shard count is 16, you need at least 1,600 distinct key values (preferably millions)."},
       {name:"Sharding by user-controllable field without hashing",desc:"If the shard key is derived directly from user-supplied data (like a username or group name) without hashing, an attacker can craft inputs that all map to the same shard, creating a targeted hot-spot DoS attack. Solution: always hash user-supplied components of the shard key. hash(username) distributes uniformly regardless of the input distribution, neutralizing the attack vector."}
     ],
     examples:[
       {name:"Twitter's user_id hash sharding",desc:"Tweets are sharded by hash(user_id). Reading a single user's tweets = single-shard query (the most common operation). Cross-user queries like assembling a home timeline (which aggregates tweets from all followed users, potentially on dozens of different shards) require multi-shard fan-out — but Twitter deliberately pre-computes timelines into a Redis cache during write-time (fan-out-on-write) so the hot read path never touches the sharded tweet store at all. The shard key optimizes for the write-and-individual-read path; the timeline-read path is served by a completely separate caching layer. This is a key lesson: you don't need your shard key to serve every query pattern. Optimize it for the most common, and build secondary indexes or caches for the rest."},
       {name:"DynamoDB hot partition errors — when theory meets production",desc:"DynamoDB partitions data by hash(partition_key). A single partition supports ~1,000 writes/sec and 3,000 reads/sec. If one partition key is too popular (e.g., a single 'global_counter' key receiving 100K writes/sec), DynamoDB returns ProvisionedThroughputExceededException even though your overall table capacity is fine. AWS's documentation explicitly warns: 'Design your partition key to distribute traffic evenly.' This is not a theoretical concern — it's the #1 cause of DynamoDB performance issues in production. Solutions: (1) Add a random suffix to spread writes: instead of partition key 'global_counter', use 'global_counter_7' through 'global_counter_255' and aggregate on read. (2) Use DynamoDB's adaptive capacity (2019 feature) which automatically redistributes capacity to hot partitions — but this has limits and doesn't fix fundamentally bad key design."}
     ],
     sizes_cfg:{
       large:{range:"Horizontal sharding required — you've exhausted vertical scaling and read replicas",rec:"Composite key design is almost always the right answer at this tier. Pattern: entity_id (high cardinality, your primary access pattern) combined with a time or sub-bucket component for query locality. Example for a messaging app: (channel_id, weekly_time_bucket). Example for a SaaS: (tenant_id, hash(record_id)). Avoid: local timestamps as the primary component (creates write-hot tail on the current time range), monotonically increasing IDs without hashing (same hot-tail problem), any field with fewer than 100 distinct values. Tools and technologies: Consistent hashing implementations — Ketama (originally by Last.fm, the classic consistent hashing library), Jump Consistent Hash (Google, more uniform distribution, no ring overhead), hashring (Go implementation used by many projects). Sharding frameworks: Citus (PostgreSQL extension, supports hash-based and range-based distribution, with automatic rebalancing — now part of Azure Cosmos DB for PostgreSQL; strength: stays within PostgreSQL ecosystem, supports distributed JOINs; weakness: some PostgreSQL features not supported in distributed mode). Vitess (MySQL sharding middleware, VSchema defines shard routing, supports online resharding; strength: battle-tested at YouTube scale; weakness: MySQL-only, steep learning curve). Cassandra partition keys (the first part of the PRIMARY KEY is the partition key and determines shard placement; strength: built-in, no external routing; weakness: once set, changing requires full data migration).",tools:["Consistent hashing","Citus shard keys","Cassandra partition keys","Vitess vschema"]},
       hyper:{range:"Global multi-datacenter sharding — data must be distributed across continents",rec:"At this tier, your shard key must account for geographic affinity in addition to load distribution. Pattern: (region_bucket, entity_id, sub_bucket). region_bucket places data near the users who access it most (EU users' data on EU shards, US users' on US shards — critical for latency and compliance). entity_id provides cardinality within each region. sub_bucket = hash(user_id) % K where K is proportional to the region's traffic share — a region handling 60% of traffic gets K=6 sub-shards while a region handling 10% gets K=1. This weighted distribution prevents large-region hot spots. Weighted consistent hashing allows adaptive rebalancing: when you add capacity to a region, the consistent hash ring redistributes only ~1/N of data. Technologies: Jump consistent hash (Google, 2014 — O(ln(n)) time, perfectly uniform, no memory overhead, used in Google's Maglev load balancer), DynamoDB Global Tables (managed multi-region — each region has a full read/write replica, conflicts resolved by last-writer-wins on item-level timestamps; strength: zero ops, automatic replication; weakness: eventual consistency only, expensive at $0.000975/replicated write unit). Cassandra token ring (distributes data across a virtual ring of tokens; each node owns a token range; vnodes allow fine-grained distribution; strength: proven at massive scale at Netflix, Apple, Discord; weakness: operational complexity of managing token ranges). Google Spanner (automatic geographic partitioning with zone-aware placement; strength: global strong consistency via TrueTime; weakness: cost, Google Cloud only).",tools:["Jump consistent hash","Google Maglev","DynamoDB Global Tables","Cassandra token ring","Spanner"]}
     },
     levelUp:[
       {from:"large",to:"hyper",trigger:"Single datacenter carries >50% of writes (creating a geographic hot spot) or cross-continent latency is violating SLOs",action:"Add geographic sub-sharding: modify your composite key to include a region or datacenter prefix so data is placed near the users who access it. Implementation: (1) Define region buckets (e.g., 'eu', 'us', 'apac') based on your user distribution. (2) Add sub_bucket = hash(user_id) % K as the third composite component, where K is weighted by the region's traffic share. Example: EU handles 40% of traffic → K=4 sub-shards per entity in EU, US handles 50% → K=5, APAC handles 10% → K=1. (3) Update your routing layer to resolve region from the request (GeoDNS or user metadata) and route to the correct regional shard group. (4) For cross-region reads (e.g., a US user viewing an EU user's profile), the router transparently sends the query to the EU shard group — the caller doesn't need to know. Fan-out cost is bounded to K shards per region per query. Technologies to implement: Vitess with region-aware VSchema, CockroachDB's locality-optimized tables (automatically places leaseholders near the users who access them most), or Cassandra with NetworkTopologyStrategy (ensures each rack/DC has the specified replica count)."}
     ],
     children:[]},

    {name:"Replication Model",id:"shard-repl",icon:"📡",phase:"Sharding",color:"#d97706",
     sizes:["small","medium","large","hyper"],short:"Single → replicas → Raft leader → leaderless quorum — how your data survives hardware failure",
     detail:{
       what:"Replication is how data is copied across multiple nodes for two purposes: durability (data survives hardware failure) and availability (the system keeps serving during node outages). There are four main models, each with increasing sophistication: (1) Single node + backups: one database instance, periodic snapshots (daily pg_dump or RDS automated snapshots). Recovery means restoring a backup — RPO is hours (data since last backup is lost), RTO is 30–60 minutes. (2) Asynchronous replication: primary node streams changes to one or more replicas in the background. Replicas are slightly behind (milliseconds to seconds typically). If the primary dies, a replica is promoted to primary — either manually (operator runs a command) or semi-automatically (health check detects failure, automation promotes). RPO = replication lag at crash time (seconds of data potentially lost). RTO = failover time (manual: minutes; automated: 30–60 seconds). (3) Raft/Paxos consensus: a distributed consensus protocol elects a leader for each data partition. All writes go through the leader, who replicates to a majority of followers before acknowledging the write. If the leader dies, the remaining nodes elect a new leader automatically within seconds. RPO = 0 (writes are replicated to majority before acknowledgement — no data loss). RTO = election timeout (typically 5–15 seconds). (4) Leaderless quorum (Dynamo-style): there's no designated leader. Any node can accept writes. Consistency is tuned via quorum parameters: with N replicas, write to W nodes and read from R nodes. If W+R > N, at least one node in the read set has the latest write — giving you strong consistency. If W+R ≤ N, you get eventual consistency but higher availability. RPO depends on W (W=ALL → RPO=0; W=1 → RPO = entire dataset on the failed node). Conflict resolution needed for concurrent writes to the same key.",
       why:"Every hard drive fails eventually — enterprise SSDs have a ~2–5% annual failure rate, and servers have ~5–10% annual failure rate including all hardware components. In a 100-node cluster, you should expect 5–10 node failures per year. Without replication, each failure is an outage lasting 30–60 minutes (time to restore from backup) with potential data loss (everything since the last backup). With replication factor 3, you can lose 2 nodes simultaneously with zero data loss and zero downtime — the remaining replica(s) continue serving while failed nodes are replaced in the background. Each replication model trades off three things: (1) write latency — synchronous replication adds a network round-trip to every write (the primary waits for replica acknowledgement before telling the client 'success'); (2) failover speed — Raft auto-elects in seconds, manual promotion takes minutes; (3) consistency guarantees — leader-based systems offer strong consistency (all reads go through the leader), leaderless offers tunable consistency. Choosing the wrong model: too weak (single node for critical financial data) risks data loss that ends the business. Too strong (synchronous replication across continents for a view counter) wastes 80ms per write for data where loss of a few writes is irrelevant.",
       numbers:"RPO (Recovery Point Objective — how much data loss is tolerable) by model: backups-only = hours (time since last backup, typically 24h for daily backups, 1h for hourly WAL archiving). Async replication = seconds to minutes (the replication lag at the moment of crash — typically 100ms–1s under normal conditions, but can grow to minutes under heavy load). Synchronous replication = 0 (the write is confirmed on multiple nodes before the client gets a response; no data can be lost). RTO (Recovery Time Objective — how long until service is restored) by model: manual failover from backup ~30–60 minutes (restore, apply WAL, configure networking). Manual replica promotion ~5–15 minutes (operator runs promotion command, updates DNS/routing). Automated multi-AZ failover (RDS Multi-AZ, Patroni) ~30–60 seconds (health check detects, automation promotes replica, application reconnects). Raft auto-election ~5–15 seconds (remaining nodes vote for new leader, clients redirect). Leaderless = no failover needed (any surviving node continues serving, no leader concept to replace). Replication factor 3 is the industry standard: survives 2 simultaneous failures, costs 3× storage, provides 2 read replicas for scaling. Write amplification: factor 3 means every 1 write becomes 3 writes (once to each replica) — plan your disk throughput accordingly."
     },
     consistencySpectrum:[
       {name:"Linearizable (strongest)",desc:"Every read returns the most recent write, globally. Equivalent to a single-copy system. Requires consensus (Raft/Paxos) on every write. Cost: highest latency, lowest throughput. Used by: etcd, ZooKeeper, CockroachDB, Spanner. When to use: leader election, distributed locks, financial ledgers — anywhere 'stale read = catastrophic.'"},
       {name:"Sequential consistency",desc:"All nodes see operations in the same order, but that order may lag real-time slightly. Less expensive than linearizable because you don't need real-time ordering. Used by: ZooKeeper for most reads (reads can be served from any node but reflect a consistent snapshot)."},
       {name:"Read-your-writes / session consistency",desc:"You always see your own most recent writes, but may see others' stale data. The practical sweet spot for most applications. Implementation: route post-write reads to the primary (stickiness) or use a monotonic read token. Used by: most web applications via session stickiness."},
       {name:"Eventual consistency (weakest useful)",desc:"Given no new writes, all replicas will eventually converge. No guarantee about when. Reads may return stale data for an undefined window. Cheapest and fastest. Used by: DNS propagation, CDN caching, Cassandra with W=1/R=1, DynamoDB default. When to use: like counts, view counts, analytics, recommendation caches — anywhere staleness is a minor UX annoyance, not a correctness violation."}
     ],
     tradeoffs:[{axis:"Durability vs Write latency",left:"Synchronous replication: every write confirmed on multiple nodes before ACK — RPO=0, but each write pays a network round-trip (1–5ms intra-AZ, 10–80ms cross-region)",right:"Asynchronous: write ACKed immediately on primary, replicated in background — faster writes, but crash before replication = lost writes (RPO = lag window, typically seconds)",pos:0.5},{axis:"Leader-based vs Leaderless",left:"Raft/Paxos leader: all writes go through one node — clean consistency model, easy to reason about, but leader is a write bottleneck and failover takes seconds",right:"Leaderless quorum (Dynamo-style): any node accepts writes — no single bottleneck, always available — but concurrent writes to the same key create conflicts that need resolution (LWW, vector clocks, CRDTs)",pos:0.5}],
     pitfalls:[
       {name:"Replication lag in a monitoring blind spot",desc:"Your replica is 30 minutes behind the primary because a long-running query on the replica blocked the WAL replay. Reads from the replica return data that's 30 minutes stale. You discover this not through alerting but through a customer support ticket saying 'my changes disappeared.' The fix is simple but critical: monitor replication lag explicitly (pg_stat_replication in PostgreSQL, SHOW SLAVE STATUS in MySQL, CloudWatch ReplicaLag for RDS). Alert when lag exceeds your staleness budget — typically 5 seconds for general reads, 1 second for session-consistent reads. Tools: Datadog's PostgreSQL integration graphs replica lag out of the box; PgHero provides a simple dashboard. At large scale, use Prometheus with custom queries on pg_stat_replication."},
       {name:"Failover during a network blip — the split-brain catastrophe",desc:"A brief network glitch between primary and replica. The health check declares the primary dead and promotes the replica. The primary recovers and doesn't know it's been replaced — now you have two nodes accepting writes, and they're diverging (split-brain). When the network heals, you have two versions of every record that was written during the split. This is the most dangerous failure mode in replicated systems. Solutions: (1) Use proper consensus protocols (Raft, Paxos) that require a majority to elect a leader — split-brain is mathematically impossible with quorum. (2) Fencing tokens: the old primary must acquire a monotonically increasing token to accept writes; the new primary has a higher token, so any late writes from the old primary are rejected. (3) STONITH (Shoot The Other Node In The Head): when a node suspects it's been replaced, it fences itself off (shuts down or isolates network) to prevent dual-writes. Tools: Patroni (PostgreSQL HA manager, uses etcd/ZooKeeper/Consul for consensus, prevents split-brain by requiring leader lock), Orchestrator (MySQL, uses Raft for consensus)."},
       {name:"Read-your-writes broken on replicas — the disappearing data illusion",desc:"User writes a comment → response says 'success' → user refreshes the page → page loads from a replica that's 500ms behind → comment is missing → user writes it again → eventually two identical comments appear. This erodes user trust fast. Solutions in order of robustness: (1) After a write, set a cookie/session flag with the write timestamp; for the next N seconds, route that user's reads to the primary (implement via sticky sessions or custom routing middleware). (2) Use PostgreSQL's synchronous_commit = remote_apply for critical writes (the primary waits until the write has been applied on at least one replica before ACKing — adds ~1–5ms but guarantees read-your-writes from any replica). (3) In the API response to the write, return the written data directly (using SQL RETURNING clause or equivalent) and have the client display it from the response rather than re-fetching."},
       {name:"Quorum settings nobody understands — the configuration minefield",desc:"Cassandra's tunable consistency seems flexible — and it is, to the point of being dangerous. W=ONE, R=ONE → fastest possible reads and writes, but no consistency guarantee whatsoever (you might read stale data indefinitely). W=ALL, R=ALL → strongest consistency, but a single node failure makes the entire partition unavailable (you need ALL nodes to respond). W=QUORUM, R=QUORUM with N=3 → reads from 2 of 3 nodes, writes to 2 of 3. Since 2+2=4 > 3=N, at least one node in the read set must have the latest write. This is the safe default for most use cases. Document your choice AND the reasoning. When new engineers join, they should understand why W=QUORUM was chosen, not just that it was. Cassandra also supports LOCAL_QUORUM (quorum within the local datacenter only) which provides strong local consistency with eventual cross-datacenter consistency — the right choice for most multi-DC deployments."}
     ],
     examples:[
       {name:"GitHub's MySQL failover with Orchestrator + Raft",desc:"GitHub's MySQL infrastructure uses Orchestrator (a MySQL HA and failover tool) running on a Raft consensus group. When the primary MySQL server fails, Orchestrator detects the failure within ~5 seconds, the Raft group elects a new Orchestrator leader (if the detecting node was also the Orchestrator leader), and Orchestrator promotes the most up-to-date replica to primary within ~10–30 seconds total. GitHub has published detailed blog posts and post-mortems showing that even with mature tooling, failover under heavy load is complex: the new primary may have slightly different data than the old one (depending on replication mode), clients need to reconnect, and the promotion process itself can briefly overload the new primary. Lesson: automatic failover is a production capability that requires investment and testing, not a checkbox you set once."},
       {name:"Cassandra's tunable consistency at Netflix — per-keyspace, per-query tuning",desc:"Netflix runs one of the world's largest Cassandra deployments. Different keyspaces (logical databases within Cassandra) use different consistency levels based on the data's importance. Payment and subscription data: W=LOCAL_QUORUM, R=LOCAL_QUORUM (strong within datacenter). Viewing history and recommendations: W=LOCAL_ONE, R=LOCAL_ONE (eventual, prioritizing availability and speed). Bookmarks and resume-position: W=LOCAL_QUORUM, R=LOCAL_ONE (durable writes, fast reads with acceptable staleness). This per-workload tuning is the mature approach: rather than one consistency level for everything, match each data type's importance to its consistency cost. Netflix documented this approach in multiple talks and blog posts."}
     ],
     sizes_cfg:{
       small:{range:"Single node with automated backups — the starting point for every system",rec:"Use a managed database with automated daily backups and point-in-time recovery (PITR). AWS RDS does this automatically: continuous WAL archiving to S3 with 35-day retention, and you can restore to any second within that window. RPO = 5 minutes (RDS backup frequency), RTO = 15–30 minutes (time to restore a new instance from backup). Google Cloud SQL HA provides similar capability. If you're self-managing PostgreSQL: set up WAL archiving to S3 (using pgBackRest or wal-g), take a base backup weekly, and test restore quarterly. The most important thing at this tier: actually test your backup restore. A backup that can't be restored is not a backup. Schedule a quarterly 'restore drill' where you restore from backup to a temporary instance and verify the data. Key technologies: AWS RDS automated backups (free, included with any RDS instance — the easiest way to get PITR), pgBackRest (most popular PostgreSQL backup tool for self-managed, supports incremental backups, parallel restore, S3 storage), wal-g (Wal-E successor, simpler than pgBackRest, good for smaller setups), Cloud SQL automated backups (Google Cloud equivalent).",tools:["RDS automated backups","pgBackRest","wal-g","Cloud SQL"]},
       medium:{range:"Primary + 1–2 async read replicas — the first step into replication",rec:"Asynchronous streaming replication: the primary continuously sends its WAL (Write-Ahead Log) to replicas, which replay it to stay up-to-date. Replicas typically lag 100ms–1s behind under normal load. Read traffic can be directed to replicas to offload the primary. For HA: one replica in a different AZ (Availability Zone) serves as the failover target. If the primary fails, this replica is promoted — either manually (you run a command) or automatically (using Patroni, RDS Multi-AZ, or Cloud SQL HA). Monitor replication lag obsessively — alert if it exceeds 5 seconds. Key technologies: AWS RDS Multi-AZ ($2× single-AZ cost, but provides automated failover within 30–60 seconds — the primary and standby are in different AZs; on failure, RDS flips the DNS endpoint to the standby with no application changes. Strength: zero ops, fully managed; weakness: standby isn't readable — it's only for failover, not read scaling. For read scaling, add a Read Replica separately). Google Cloud SQL HA (similar to RDS Multi-AZ, regional instance with automatic failover). Patroni (open-source PostgreSQL HA manager — uses etcd/ZooKeeper/Consul as a distributed configuration store to manage leader election and failover. Strength: self-hosted flexibility, battle-tested at GitLab, Zalando; weakness: you're managing the complexity yourself). pg_replication_slots (PostgreSQL feature ensuring the primary keeps WAL segments until the replica has consumed them — prevents replica from falling too far behind, but can cause primary disk to fill if replica is disconnected for too long — monitor slot lag).",tools:["RDS Multi-AZ","Cloud SQL HA","Patroni","pg_replication_slots"]},
       large:{range:"Raft-based leader election per shard — automatic failover in seconds, no human in the loop",rec:"Each data shard runs a Raft consensus group with 3 or 5 nodes. The Raft protocol: (1) one node is elected leader; (2) leader handles all writes; (3) writes are replicated to a majority of followers before being committed; (4) if the leader fails, the remaining nodes hold an election and a new leader is serving within 5–15 seconds. This gives you RPO=0 (writes confirmed on majority), RTO=5–15 seconds (election timeout), and strong consistency (all reads from the leader see the latest write). Reads can optionally go to followers for scaling, but follower reads may be slightly stale (one replication round-trip behind). Key technologies: etcd (the consensus store used by Kubernetes — Raft-based, designed for configuration and coordination data, not general-purpose storage; strength: extremely well-tested, the backbone of every K8s cluster; weakness: designed for small datasets <8GB, not for application data). CockroachDB (uses Raft for every range/shard of data, fully automated — you don't manage Raft yourself; strength: strong consistency, automatic sharding and rebalancing, PostgreSQL wire-compatible; weakness: higher write latency than single-node Postgres ~5–10ms per write due to consensus, operational complexity for self-hosted). TiKV (the distributed KV store under TiDB, also Raft-based; strength: high write throughput, used at massive scale in China; weakness: less mainstream in Western ecosystem). Patroni + etcd/ZooKeeper (for PostgreSQL HA with consensus-based leader election; strength: brings Raft-like failover to vanilla PostgreSQL; weakness: doesn't shard — still single-writer, just with automatic failover).",tools:["etcd","CockroachDB (Raft)","TiKV","Patroni + ZooKeeper"]},
       hyper:{range:"Leaderless quorum OR multi-region Raft — for systems that must never go down and span the globe",rec:"At this tier, choose based on your consistency requirement: (A) Leaderless quorum (Dynamo-style): Used when availability is paramount and eventual consistency is acceptable. Any node accepts writes. Consistency tuned via W (write quorum), R (read quorum), N (replica count). The safe default: W=QUORUM (ceil(N/2)), R=QUORUM, N=3 — gives W=2, R=2, which guarantees overlap (2+2>3) for strong consistency. For higher availability: W=1, R=N reads from all replicas and takes the latest — fast writes, strong reads, but any single node failure blocks reads. Read repair: when a read discovers stale replicas, it pushes the latest value to them. Anti-entropy: background process (Merkle trees) continuously compares replicas and heals divergence. Conflict resolution for concurrent writes: Last-Writer-Wins (LWW) using timestamps (simple but can lose data if clocks are skewed), vector clocks (tracks causality, more correct but more complex, used by Riak), CRDTs (Conflict-free Replicated Data Types — mathematical structures that merge automatically: G-Counters for increment-only counters, OR-Sets for add/remove sets, LWW-Registers for simple values). (B) Multi-region Raft: Used when global strong consistency is required (financial systems, inventory). Each Raft group spans 3–5 regions. Writes pay cross-region latency (~80–200ms for transcontinental quorum). Google Spanner takes this approach using TrueTime (GPS + atomic clocks) to bound clock uncertainty and enable lock-free globally-ordered transactions. CockroachDB offers a similar model without custom hardware (uses NTP with bounded uncertainty, slightly higher latency). Key technologies: Cassandra (the canonical leaderless quorum implementation — tunable W/R per query, vnodes for distribution, Merkle trees for anti-entropy; strength: proven at massive scale at Apple, Netflix, Discord; weakness: operational complexity, JVM tuning, compaction storms). DynamoDB (managed leaderless, partition-key based; strength: zero ops, unlimited scale; weakness: eventual consistency by default, strongly consistent reads cost 2× and are limited). Riak (the original Dynamo-paper implementation, with vector clocks and CRDTs built in; strength: most theoretically sound conflict resolution; weakness: smaller community, limited commercial support). Custom CRDT libraries: Yjs (JavaScript, for collaborative editing), Automerge (general-purpose CRDT, Rust+WASM), Redis CRDTs (available in Redis Enterprise for geo-distributed counters and sets).",tools:["Cassandra (quorum)","DynamoDB","Riak","Google Spanner","CockroachDB multi-region","CRDT libraries"]}
     },
     levelUp:[
       {from:"small",to:"medium",trigger:"Recovery time >1 hour is unacceptable OR read load on the primary is causing latency spikes (CPU >70%, p99 >200ms)",action:"Add a read replica and configure your application to route read queries to it. Here's how, step by step: (1) In AWS RDS: create a Read Replica from the console or CLI — RDS handles replication setup automatically, takes ~15 minutes. (2) Configure your application's database connection: use a separate connection string for reads (pointing to the replica endpoint) and writes (pointing to the primary endpoint). Many ORMs support this natively: Rails has 'connects_to' for read/write splitting, Django has DATABASE_ROUTERS, SQLAlchemy has 'binds'. (3) For HA (not just read scaling): enable Multi-AZ on RDS, which creates a synchronous standby in a different AZ with automatic failover — the endpoint DNS flips automatically on primary failure. (4) Set up replication lag monitoring: in Datadog, enable the PostgreSQL integration and alert if 'postgresql.replication_delay' exceeds 5 seconds. In CloudWatch, monitor the 'ReplicaLag' metric. (5) Implement read-your-writes: after any write request, set a response header or cookie ('X-Read-From-Primary: true') and have your middleware route that user's reads to the primary for the next 5 seconds. This prevents the 'disappearing data' problem."},
       {from:"medium",to:"large",trigger:"Manual failover is too slow for your SLO (RTO >5 minutes) OR you're sharding and need per-shard automated failover",action:"Implement consensus-based automatic failover. The two main paths: (A) If you're on PostgreSQL: deploy Patroni with etcd. Here's how: (1) Install etcd as a 3-node cluster (runs on small instances, ~500MB RAM each). (2) Install Patroni on each PostgreSQL node — Patroni manages leader election via etcd, configures streaming replication automatically, and promotes replicas on leader failure within 10–30 seconds. (3) Configure your application to discover the current leader via Patroni's REST API, or use a connection pooler like PgBouncer that follows the Patroni leader. (4) Test failover monthly: use 'patronictl failover' to trigger a manual failover and verify your application reconnects cleanly. GitLab and Zalando both publish detailed Patroni deployment guides. (B) If you're adopting a distributed database: CockroachDB and TiDB have Raft built in — you deploy 3+ nodes, and leader election, replication, and failover are fully automatic with zero configuration. CockroachDB: 'cockroach start --join=node1,node2,node3' and you have a Raft group. TiDB: deploy via TiUP (the official deployment tool). Both give you RPO=0 and RTO=5–15 seconds out of the box."},
       {from:"large",to:"hyper",trigger:"Multi-region writes are required (users on multiple continents need low-latency write access) OR partition tolerance is critical (your system must remain available even if an entire region goes offline)",action:"Transition to leaderless quorum or multi-region consensus. The two main paths: (A) Leaderless (Cassandra/DynamoDB): (1) Deploy Cassandra with NetworkTopologyStrategy, replication_factor=3 per datacenter. This gives you local strong consistency (LOCAL_QUORUM) with async cross-DC replication. (2) For each table, determine the right consistency level: financial data → LOCAL_QUORUM writes + LOCAL_QUORUM reads; user content → LOCAL_ONE writes + LOCAL_ONE reads; counters → use Cassandra's counter type (CRDT-based, merges automatically). (3) Implement read-repair and anti-entropy: Cassandra does this automatically, but tune gc_grace_seconds (the window during which tombstones are kept for consistency — default 10 days; too short → zombie data returns, too long → tombstone accumulation). (4) For conflict resolution beyond LWW: implement application-level merge logic or use CRDTs. Redis Enterprise supports CRDTs for counters and sets across geo-distributed clusters. Riak provides vector clocks for causal conflict detection. (B) Global strong consistency (Spanner/CockroachDB): (1) Google Spanner: create a multi-region instance (configuration presets like 'nam-eur-asia1' span 3 continents). Writes to the leader region are fast (~5ms); writes requiring cross-region consensus are ~100–200ms. Reads from any region can be strongly consistent (via 'stale reads' with bounded staleness) or exactly consistent (via read-write transactions). (2) CockroachDB: deploy nodes across regions, configure locality-optimized tables so each table's leaseholder (leader) is in the region closest to its primary users. Cross-region writes pay consensus latency, but most reads are local. (3) For both: implement per-table/per-feature consistency policies. Not everything needs global strong consistency — identify the 5% that does (financial, inventory) and use eventual for the rest."}
     ],
     children:[]},

    {name:"Routing Layer",id:"shard-route",icon:"🗺️",phase:"Sharding",color:"#d97706",
     sizes:["medium","large","hyper"],short:"How requests find the right shard — the nervous system of your distributed database",
     detail:{
       what:"The routing layer is the mechanism that takes an incoming query and determines which shard owns the relevant data, then sends the query to that shard and returns the result. Without it, every client application would need to understand the shard map (which key ranges live on which nodes) and update that understanding whenever you add, remove, or rebalance shards — an operational impossibility at scale. There are three common routing patterns: (1) Stateless proxy (e.g., MongoDB's mongos, Vitess's vtgate, ProxySQL): a pool of proxy servers sits between clients and shards. Each proxy holds a cached copy of the shard map (fetched from a metadata store like etcd or ZooKeeper). The client sends queries to the proxy, which parses the query, extracts the shard key, looks up the target shard, forwards the query, and returns the result. The proxy is stateless — any proxy can handle any request — so you run multiple behind a load balancer for redundancy. Overhead: 1–2ms per query (network hop + parsing). (2) Gossip protocol (e.g., Cassandra): every database node knows the complete topology. When a client connects to any node, that node can route the query to the correct owner — or, if the client happened to connect to the owner, serve it directly. Topology changes propagate via gossip (nodes periodically exchange topology information with random peers) and converge within ~1–3 seconds. No external metadata store needed. Overhead: 0ms if client connects to owner, 1–2ms if forwarded. (3) Smart client (e.g., Redis Cluster, Kafka client): the client library itself holds a local copy of the shard map and connects directly to the owner node — zero routing overhead. If the topology changes (a shard moved), the client discovers this when the node responds with a MOVED redirect, at which point the client updates its local map and retries. The client periodically refreshes the map in the background to stay current. Overhead: 0ms (direct connection). Complexity: client library is more sophisticated.",
       why:"The routing layer is the traffic cop of your distributed database. Without it, clients would need to: (1) know which shard owns which key ranges, (2) maintain connections to all shards, (3) update their knowledge when shards are added, removed, or rebalanced, and (4) handle shard failures and failover. That's an enormous burden to push onto every application that talks to the database. The routing layer centralizes this complexity — applications talk to a simple endpoint, and the routing layer handles the rest. But the routing layer introduces its own concerns: it's an additional network hop (latency), an additional component that can fail (availability), and an additional system that must be kept up-to-date (consistency of the shard map). If the routing layer goes down, nothing can reach any shard — you've replaced one SPOF (the database) with another (the router). If the shard map is stale (the router thinks shard 5 owns a key that was moved to shard 12 during resharding), queries go to the wrong place. The routing layer must be at least as available and correct as the data layer it fronts.",
       numbers:"Latency overhead by pattern: stateless proxy adds 1–2ms per hop (network transit + query parsing + shard map lookup). For a simple key-value lookup that takes 1ms on the shard, the proxy doubles the total latency. For a complex query that takes 50ms, the proxy adds negligible overhead. Smart client: 0ms overhead (direct connection, no intermediary). Gossip-routed (Cassandra coordinator): 0ms if client's connection node is the owner, 1–2ms if forwarded internally. Topology change propagation speed: gossip protocol ~1–3 seconds for full convergence, etcd/ZooKeeper watch notifications ~100–500ms, smart client MOVED redirect + map refresh ~immediate (one retry). Routing throughput capacity: stateless proxy (Vitess vtgate) handles ~50K–200K queries/sec per instance; scale by adding more instances behind a load balancer. Smart client throughput is limited only by the client application's own concurrency and the target shard's capacity."
     },
     routingPatternComparison:[
       {name:"Stateless Proxy",pros:"Simple client (talks to a single endpoint, doesn't know about shards), easy to add cross-cutting concerns (query logging, rate limiting, query rewriting), centralized control of routing logic",cons:"Additional network hop (1–2ms), proxy is a potential bottleneck and SPOF (must run multiple instances), proxy must parse every query to extract the shard key (CPU overhead)",bestFor:"Moderate shard counts (<100), teams that want centralized routing control, MySQL/PostgreSQL sharding (Vitess, ProxySQL)",tools:["Vitess vtgate","MongoDB mongos","ProxySQL","custom stateless router"]},
       {name:"Gossip Protocol",pros:"No external metadata store, no SPOF, every node can route — extremely resilient. Topology changes propagate automatically without central coordination",cons:"Convergence takes 1–3 seconds (during which some nodes may have stale topology), gossip traffic grows with cluster size (each node exchanges info with random peers periodically), harder to debug routing decisions",bestFor:"Large clusters (100+ nodes), systems where any-node-can-route simplifies client logic, Cassandra/ScyllaDB deployments",tools:["Cassandra gossip","Consul gossip","Serf","custom gossip protocol"]},
       {name:"Smart Client",pros:"Zero routing overhead (direct connection to owner shard), no intermediary to fail, highest possible throughput (limited only by shard capacity)",cons:"Client library is complex (must hold shard map, handle MOVED redirects, manage connections to all shards), every language needs a smart client implementation, shard map in the client can be briefly stale after topology changes",bestFor:"Ultra-low-latency requirements, very large clusters where proxy would be a bottleneck, Redis Cluster and Kafka deployments",tools:["Redis Cluster smart client (redis-py-cluster, jedis, ioredis)","Kafka client (librdkafka)","custom shard-aware client"]}
     ],
     tradeoffs:[{axis:"Centralized vs Distributed routing",left:"Proxy: centralized control, easy to monitor and debug, but single choke point (run multiple for HA) and adds latency",right:"Gossip/Smart client: no SPOF, no extra hop — but routing logic is spread across many nodes/clients, harder to audit and debug",pos:0.5},{axis:"Routing freshness vs Latency",left:"Synchronous shard map lookup (etcd read on every query): always current map, but adds 1–5ms per query",right:"Cached shard map with async refresh: 0ms overhead, but may route to stale location during topology changes (handle with MOVED redirect and retry)",pos:0.5}],
     pitfalls:[
       {name:"Proxy as bottleneck — the hidden scaling ceiling",desc:"All database traffic flows through the proxy tier. You have 200K QPS of database traffic and your proxy instances can handle 50K QPS each — you need 4 proxies minimum (with headroom, 6). If one proxy fails, the remaining 3 are at 67K QPS each — over capacity. You've created a fragile choke point. Solutions: (1) Over-provision the proxy tier to handle N-1 failures (if you have 4 proxies, size each for 67K so that 3 can handle the full load). (2) Autoscale the proxy tier like you autoscale application servers. (3) At very high scale (>500K QPS), consider moving to smart client or gossip to eliminate the proxy entirely."},
       {name:"Stale shard map after resharding — the misdirected query",desc:"You resharded: data for key range 1000–2000 moved from shard 5 to shard 12. The proxy's cached shard map still says 'range 1000–2000 → shard 5.' A query for key 1500 goes to shard 5, which doesn't have that data anymore. Solutions: (1) During resharding, keep a forwarding rule on the old shard ('if you receive a query for a range you no longer own, forward it to the new owner'). This works but adds latency during the migration period. (2) Use a shard map with a version number; after updating the map, broadcast an invalidation to all proxies/clients. (3) Use consistent hashing, which minimizes the amount of data that moves when the topology changes and reduces the window of stale routing. Technologies: Vitess handles this automatically via its VSchema and topology server (etcd-backed, change-notified). MongoDB's config servers serve the authoritative chunk map; mongos instances cache it and refresh on StaleShardVersion errors."},
       {name:"Routing logic embedded in every application service",desc:"Instead of a centralized proxy or shared client library, each of your 30 microservices has its own shard-routing code — some using one algorithm, some another, some with bugs. When you need to reshard, you have to update and redeploy all 30 services. This is the 'distributed monolith' pattern applied to routing. Solution: centralize routing logic either in a proxy tier (Vitess, ProxySQL) or in a shared client library that all services import. The library should be versioned, tested, and published as an internal package. Changes to shard map or routing algorithm happen in one place and propagate to all services on their next deploy."}
     ],
     examples:[
       {name:"MongoDB mongos — the canonical stateless proxy",desc:"MongoDB's sharded cluster uses mongos as the query router. Clients connect to one or more mongos instances (which are stateless and horizontally scalable). mongos reads the shard map from the Config Server Replica Set (a 3-node Raft group that stores the authoritative shard metadata). When a query arrives, mongos: (1) parses the query to extract the shard key, (2) looks up the target shard in its cached map, (3) forwards the query, (4) returns the result. If the shard key is present, the query targets a single shard (targeted query). If the shard key is absent, mongos sends the query to ALL shards and merges results (scatter-gather) — this is why shard key design matters. mongos refreshes its map from Config Servers when it encounters a StaleShardVersion error (the shard itself tells mongos 'your map is outdated'). Strengths: simple for applications (connect to mongos like a single MongoDB instance), handles cross-shard queries automatically. Weaknesses: additional hop, proxy must parse every query, mongos itself can be a bottleneck at very high QPS."},
       {name:"Redis Cluster's smart client — zero overhead routing",desc:"Redis Cluster uses the smart client pattern: the client library (jedis for Java, ioredis for Node.js, redis-py for Python) holds the slot map (16,384 slots distributed across nodes) locally. When the client sends a command, the library computes CRC16(key) % 16384 to determine the slot, looks up which node owns that slot, and sends the command directly — zero intermediary, zero extra hops. If the topology changed and the client's map is stale, the node returns a MOVED redirect: 'this slot now lives on node 10.0.0.5:6379.' The client updates its local map and retries. ASK redirects handle the in-progress migration case (the slot is being moved but hasn't finished). This pattern gives Redis Cluster the lowest possible routing latency, at the cost of client library complexity — every language SDK must implement slot mapping, MOVED handling, and background map refresh. Strengths: zero-overhead for the common case, no SPOF in the routing path. Weaknesses: each language needs a correctly-implemented smart client (bugs in the client = data routed to wrong shard), clients maintain connections to all nodes (connection count = clients × nodes)."}
     ],
     sizes_cfg:{
       medium:{range:"2–10 shards — simple topology, moderate query volume",rec:"Stateless proxy router is the right choice at this scale. Run 2–3 proxy instances behind a load balancer for HA. The proxies cache the shard map from a metadata store (etcd or ZooKeeper) and refresh on change notification or error. Stale routes trigger automatic retries — the proxy detects a 'wrong shard' error and re-fetches the map. This is the 'set it and forget it' tier of routing. Key technologies: Vitess vtgate (if you're on MySQL — handles query routing, cross-shard queries, and online resharding; deploy 3 vtgate instances behind an ALB). ProxySQL (MySQL query router, lighter than Vitess, good for simple read/write splitting and basic shard routing; strength: low overhead, widely used; weakness: no online resharding support). MongoDB mongos (built into MongoDB sharded clusters, deployed alongside your application). Custom stateless router (if you're on PostgreSQL with manual sharding — a thin Go/Rust proxy that hashes the shard key and forwards; Citus handles this internally if you use Citus).",tools:["mongos (MongoDB)","Vitess vtgate","ProxySQL","custom stateless router"]},
       large:{range:"10–500 shards — topology changes are frequent, routing must be resilient",rec:"At this scale, consider gossip-based routing to eliminate the proxy as a potential bottleneck and SPOF. In a gossip system, every database node maintains the full topology map via periodic peer-to-peer exchange. Any node can be the 'coordinator' for any query — if the client connects to node A but the data lives on node C, node A forwards the request to C internally (one hop). The client only needs to know about a few seed nodes for initial connection; the driver discovers the rest via gossip. Key technologies: Cassandra gossip (built-in, zero configuration — each Cassandra node gossips its token ownership to peers every second; the Cassandra driver discovers all nodes from any seed. Strength: proven at Netflix, Apple, Discord at hundreds of nodes; weakness: gossip convergence takes 1–3 seconds, during which routing may be briefly incorrect). Consistent hashing ring (used by Cassandra, DynamoDB, and Riak — each node owns a range of the hash ring; virtual nodes (vnodes, typically 256 per physical node) smooth the distribution. On node addition, only ~1/N of data needs to move). etcd for shard map (if you're using a custom sharding solution — store the authoritative shard map in etcd; nodes and proxies watch for changes and update within milliseconds).",tools:["Cassandra gossip","consistent hashing ring","etcd for shard map"]},
       hyper:{range:">500 shards, global deployment — every millisecond of routing overhead matters",rec:"Smart client is the only routing pattern that achieves zero overhead at this scale. The application's database client library holds the full shard map locally, computes the target shard from the key, and connects directly to the owner node. The map is refreshed: (1) reactively on MOVED/ASK redirects (immediate correction), (2) proactively via background polling every 15–60 seconds (catch slow topology changes), and (3) via push notification from the metadata store if available. At >500 shards, the client maintains a connection pool to each shard (or each shard group) — use connection multiplexing (HTTP/2, gRPC) to keep connection count manageable. Key technologies: Redis Cluster smart client (the canonical implementation — ioredis for Node.js, jedis/lettuce for Java, redis-py-cluster for Python; each implements CRC16 slot mapping, MOVED/ASK handling, and background refresh). Kafka client metadata (Kafka's producer/consumer clients hold partition → broker mapping locally; they refresh from any broker on errors or every metadata.max.age.ms; strength: extremely mature, handles partition reassignment during broker failures transparently). Custom shard-aware client library (for custom sharding — build a library that loads the shard map from etcd/ZooKeeper, hashes the key, and routes directly; used by many large companies including Uber, Stripe, and Square for their internal sharded databases).",tools:["Redis Cluster smart client","Kafka client metadata","custom shard-aware client library"]}
     },
     levelUp:[
       {from:"medium",to:"large",trigger:">50 shards OR the proxy tier is becoming a throughput bottleneck (CPU >70%, latency added >5ms) OR proxy failover is too slow",action:"Migrate from proxy-based routing to gossip or smart client routing to eliminate the proxy as a scaling bottleneck. Here's how for each path: (A) Gossip (if using Cassandra/ScyllaDB): this is already built in — ensure your Cassandra driver (DataStax Java driver, gocql, python-driver) is configured with TOKEN_AWARE load balancing policy, which routes requests directly to the replica that owns the token. This eliminates the coordinator hop for single-partition queries. Verify with 'nodetool status' that all nodes see the same topology. (B) Smart client (if using Redis Cluster or custom sharding): update your client libraries to use the smart client mode. For Redis: switch from using a proxy (Twemproxy) to using a cluster-aware client (ioredis in cluster mode, jedis in cluster mode). The client will handle slot mapping and MOVED redirects automatically. For custom sharding: build a client library that loads the shard map from etcd on startup, refreshes it every 30 seconds and on routing errors, and connects directly to the owner shard. Publish this library as an internal package that all services consume."},
       {from:"large",to:"hyper",trigger:"Any routing latency is unacceptable (sub-millisecond SLO) OR topology change propagation must be near-instant",action:"Commit fully to smart client routing with direct shard connections. Implementation: (1) Build or adopt a shard-aware client library for each language your services use. The library must: hold the full shard map in memory, compute target shard from key in <1μs (hash + lookup), maintain connection pools to all shard groups, handle MOVED redirects gracefully (update map + retry, no error to caller), refresh map in background every 15–30 seconds, and expose metrics (cache hit rate, redirect rate, connection pool utilization). (2) For topology changes (resharding, node addition), implement a push notification system: the shard map metadata store (etcd) emits a watch event, which a sidecar or library watcher receives and applies within milliseconds. The reactive MOVED redirect serves as a safety net for any changes the push missed. (3) At this scale, connection count becomes a concern: if you have 1000 application instances each connected to 500 shards, that's 500,000 connections. Use connection multiplexing (gRPC, HTTP/2) or connection pooling (each application instance maintains a small pool to each shard — 3–5 connections — and multiplexes requests). Monitor total connection count per shard and set hard limits."}
     ],
     children:[]}
  ]
};
